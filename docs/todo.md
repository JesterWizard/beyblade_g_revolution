[] Disassemble / analyze baserom.gba and begin peeling `asm/rom.s`
[] Run `python3 tools/scan_ram_literals.py --emit-asm` and tighten IWRAM/EWRAM pools
[] Add first decomp in `src/` and wire it in `ld_script.ld`
