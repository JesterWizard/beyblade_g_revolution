# Decompilation work queue — edit pins, blockers, and WIP here.
# Refresh: python3 tools/decomp/next_queue.py --write   (or: make queue)
# Auto-ranked backlog lands in decomp-queue.md and decomp-queue.json.
# Unmatched C: [[wip]] + src/wip/  (see docs/decomp-wip.md). Do not discard drafts.

[settings]
top = 40
# prefer: battle (default) | all | smallest
prefer = "battle"

# Hand-picked targets — shown first, in file order.
# (sub_0806A6F8 / sub_08034894 unpinned — both confirmed agbcc-hard, see [[block]] below)

# Incomplete C — resume these before picking a new function. See docs/decomp-wip.md
[[wip]]
name = "sub_08046278"
seed = "src/wip/sub_08046278.c"
notes = "src/wip/sub_08046278.md"
status = "logic OK; 11/92 (56B vs 92B); extra r8/r9 leaf spill"
next = "independent per-field addressing like sub_08046230; force r8/r9 live"
score = "11/92"

# Known hard cases — excluded from "recommended next", listed under Blocked.
[[block]]
name = "sub_080601C4"
reason = "r8 pool pin — permuter best score ~100"

[[block]]
name = "sub_0802D8C4"
reason = "agbcc extra push {lr} on branch leaves"

[[block]]
name = "sub_080615EC"
reason = "branchy asm — agbcc compile fail"

[[block]]
name = "sub_08061BDC"
reason = "agbcc extra push {lr} on null-check leaf"

[[block]]
name = "sub_08062634"
reason = "agbcc extra push {lr} on null-check leaf"

[[block]]
name = "sub_08061E40"
reason = "agbcc extra push {lr} on null-check leaf"

[[block]]
name = "sub_0802B994"
reason = "Unk7709C ROM table lookup keyed on *gMainWorkPtr->unk1690 — logic reconstructed correctly (same-size DIFF, 2/58 bytes short from a missing 2B) but every do/while/goto shape tried lands agbcc on push{lr}...pop{r1};bx r1 leaf framing that retail doesn't have (bx lr direct); same agbcc-extra-push-on-leaf class as sub_0802D8C4/sub_08061BDC; needs permuter"

[[block]]
name = "sub_0804495C"
reason = "32-halfword table copy (gUnk_08094E00[gMainWorkPtr->unk181F] -> PLTT 0x050001C0) — void(void) leaf, same agbcc-extra-push-on-leaf framing as sub_0802B994 (60B retail vs 64B compiled, same shape every C form tried); needs permuter"

[[block]]
name = "sub_0802C62C"
reason = "counts gMainWorkPtr->unk1694[0..0x7F] entries with unk03==(s8)a — same-size DIFF (64B=64B!) across every declaration-order variant tried, purely a r2/r3/r4 register-choice swap (which var lands in the return register); 7000+ permuter iterations floor at score 95, never zero; needs permuter"

[[block]]
name = "sub_0803DBD0"
reason = "2D ROM table lookup (gUnk_080796DC[a][gMainWorkPtr->unk1818] / gUnk_08097458[unk1818] fallback) — retail leaf is bx lr with no push at all; every C shape tried (both branch orders) needs push{lr}/pop{r1} for one extra temp register (80B retail vs 84B compiled); same agbcc-extra-push-on-leaf class; needs permuter"

[[block]]
name = "sub_08033530"
reason = "battle state branch — subs r2 #0x6C vs direct unk201C pool (same-size DIFF)"

[[block]]
name = "sub_080473E4"
reason = "dual IWRAM zero — agbcc pool order / CSE of 0x634 and 0x63C (permuter best ~5)"

[[block]]
name = "sub_0806FEFC"
reason = "BtlObjNode move-to-tail (gBtlObjListHead/Tail) — agbcc inserts extra push {lr}/pop{r1}+bx r1 frame on this null-check leaf like sub_0802D8C4/sub_08061BDC; same-size DIFF on direct-return form, +12B with early-return form; needs permuter"

[[block]]
name = "sub_0806FF28"
reason = "BtlObj unlink+push-to-head (obj->unk19 early return) — same agbcc extra push{lr} frame quirk as sub_0806FEFC; +4B DIFF; needs permuter"

[[block]]
name = "sub_08034894"
reason = "docs/battle.md: readable Thumb — agbcc prologue / pool ordering (no struct yet for param @ +0x30C flag / +0x300,0x302,0x304 fields)"

[[block]]
name = "sub_0806A6F8"
reason = "docs/battle.md: readable Thumb — large input hub (436B, 6 IWRAM refs)"

[[block]]
name = "sub_080428C4"
reason = "docs/battle.md: readable Thumb — C adds push {lr} (same extra-prologue quirk as sub_0806FEFC family)"

[[block]]
name = "sub_080475C4"
reason = "docs/battle.md: readable Thumb — C adds push {lr} (same extra-prologue quirk as sub_0806FEFC family)"

[[block]]
name = "sub_080475F4"
reason = "docs/battle.md: readable Thumb — C adds push {lr} (same extra-prologue quirk as sub_0806FEFC family)"

[[block]]
name = "sub_08073114"
reason = "BtlObjTable scan+remove (loop over gBtlObjTable[0..gBtlObjTableCount) matching entry->key==obj, calls sub_0806A434/sub_08067B98) — logic reconstructed correctly (same-size DIFF on every variant tried) but agbcc compiles the do-while as pre-test loop + different table-pointer register placement than retail; needs permuter or deeper agbcc loop-codegen trick"

[[block]]
name = "sub_08062728"
reason = "u32 zero-fill loop (a->unk04[i]=0 for i<a->unk08) — retail uses stm r0!,{r3} leaf loop (18B), agbcc compiles any equivalent C to a push/pop-framed indexed loop (32B); needs permuter or specific idiom to trigger stm codegen"

[[block]]
name = "sub_08074144"
reason = "single instruction 'mov pc, lr' (2B) — semantically identical to bx lr but a different opcode; agbcc never emits mov pc,lr for an empty C function (only bx lr), so this must stay naked asm"

[[block]]
name = "sub_08062CF4"
reason = "BGR555 color pack (inverse of sub_08062CC8/sub_08062D24): rgb[0..2] -> u16 @ PLTT 0x05000200+idx*2 — logic reconstructed correctly (same-size DIFF, ~1 instruction reordered) across many register-pinned variants; retail keeps r6 live (push {r4,r5,r6,lr}) but my C never needed r6 pressure, changing push set; needs permuter"

[[block]]
name = "sub_08062C80"
reason = "calls _08073C4C(0, dst, size, src) at raw address 0x08073C4C twice (VRAM/PLTT clear via CpuFastSet-style primitive) — that callee has no C symbol/prototype anywhere in the codebase yet (only referenced via bl _08073C4C from naked asm in many other unconverted functions); needs the callee named/prototyped first"

[[block]]
name = "sub_08045128"
reason = "reads gMainWorkPtr->unk1688[idx].unk08 into unk1788, computes base = gMainWorkPtr->unk168C + idx*8032 (retail's literal shift/sub chain), calls sub_08045590(base,0) — logic correct (same-size DIFF, ~14/80 bytes differ) across many pointer-arithmetic orderings tried; needs permuter for the base-pointer scheduling"

[[block]]
name = "sub_08030938"
reason = "computes a->unk2D8/unk2D4/unk00->unk30/unk00->unk34/nested sub_080674A0 fixed-point calls, then sub_080346C0(a, unk30, unk34, unk2D8, 0xB4-nested) with 5th arg on stack — logic correct across several forms but push-set (r4-r7 vs r4-r6) and stack-arg store ordering differ; needs permuter"

[[block]]
name = "sub_080523A4"
reason = "calls sub_08061BE8, sub_0805264C(a,0..5), then a->unk288->unk0C = a->unk28C->unk0C = (a->unk2D5<<12)+0x2C00 — logic correct (same-size DIFF, ~15/112 bytes) across several pointer/base-register orderings; needs permuter"

[[block]]
name = "sub_080604C8"
reason = "byte-swaps 6 u8 pairs from *gUnk_03000750 into u16 fields, writes them to REG_BG palette-ish IO regs 0x04000040-0x0400004A — logic correct; real remaining gap is a 4-byte tail-fold (agbcc collapses the last out=out+2;*out=val into strh [r0,#2] when out isn't used again, unlike retail which keeps the explicit adds+strh[0]); several dependency-shape rewrites (loop, pre-increment, reordering) all land 4B short; needs permuter"

[[block]]
name = "sub_08049F98"
reason = "sound/anim trigger sequencer: sub_080617C4 x2, sub_080615EC x3 with idx<<4+8/+0x10 offsets, sub_0806171C x3 with sub_08061784()<<16>>17 — m2c fails to reconstruct (r8 stack-saved 3rd param); multiple hand-written + register-pinned C forms all land 8 bytes over; needs permuter"

[[block]]
name = "sub_0804E17C"
reason = "byte-identical to sub_08049F98 (different embedded const 0x083A83F4); same blocker"

[[block]]
name = "sub_08051578"
reason = "byte-identical to sub_08049F98 (different embedded const 0x083A85A4); same blocker"

[[block]]
name = "sub_08053218"
reason = "byte-identical to sub_08049F98 (different embedded const 0x083A8724); same blocker"

[[block]]
name = "sub_08038314"
reason = "battle countdown gate: a->unk304-- then flush-condition on gBtlKeysHeld&3, else store v to a->unk2FC and sub_08062044 x4 on gBattleWork->unk19C[0..3] — logic reconstructed correctly (same-size DIFF, 5/108 bytes), several source shapes (inline expr, cached local, array-index cast) all land agbcc on the same reordering (mask loaded+ANDed before vs after the #3 immediate load); needs permuter (base score 70, 20k+ iterations without a zero)"

[[block]]
name = "sub_08045C5C"
reason = "gMainWorkPtr->unk1710[25..26] input-repeat debouncer keyed on gBtlInputMask==0xFC00 — logic reconstructed correctly but agbcc drops r7 from the push set (r4-r6+lr, 140B) vs retail's r4-r7+lr (136B); tried inline/cached-local/branch-order variants, all land on the same 4B-over shape; needs permuter"

[[wip]]
name = "sub_0802E048"
seed = "src/wip/sub_0802E048.c"
notes = "src/wip/sub_0802E048.md"
status = "same-size DIFF, 188/228 bytes (82.5%)"
next = "try decomp-permuter register-order search; likely same agbcc leaf-fn reg-alloc quirk as dead-end list"
score = "188/228"

[[wip]]
name = "sub_080338F0"
seed = "src/wip/sub_080338F0.c"
notes = "src/wip/sub_080338F0.md"
status = "same_size DIFF, 20/104 bytes (19.2%) — structural rewrite needed, indexing bug noted in file"
next = "fix table indexing to byte-offset (see note), re-add Unk338F0Table struct + Unk33958 retype, then re-run match_function.py"
score = "20/104"

[[wip]]
name = "sub_0802D2C0"
seed = "src/wip/sub_0802D2C0.c"
notes = "src/wip/sub_0802D2C0.md"
status = "DIFF; corrected retail branch semantics, but best current compile is 46/108 and 104 bytes due pool/register layout"
next = "permuter run targeting corrected branch shape and literal-pool/register order"
score = "46/108"

[[wip]]
name = "sub_0802BC14"
seed = "src/wip/sub_0802BC14.c"
notes = "src/wip/sub_0802BC14.md"
status = "same-size DIFF (58/112, 51.8%) after signed-call and r0-pointer hints; remaining pool/register layout differs"
next = "leave readable Thumb; revisit with a verified signed-argument prototype or targeted pool/register permutation"
score = "58/112"

[[wip]]
name = "sub_0802BF04"
seed = "src/wip/sub_0802BF04.c"
notes = "src/wip/sub_0802BF04.md"
status = "best 55/140 size-mismatch; s16 comparison and explicit r8 zero-register hints did not recover retail setup/order"
next = "leave readable Thumb; revisit with targeted constant/pointer reuse or permuter"
score = "55/140"

[[wip]]
name = "sub_08035C64"
seed = "src/wip/sub_08035C64.c"
notes = "src/wip/sub_08035C64.md"
status = "same_size DIFF 85/182 (46.7%); logic fully correct (repulsion physics on Unk346C0Inner, dx/dy/distSq/threshold gate) but agbcc always emits the early-return (zero) block first regardless of if/else source order — branch layout inverted vs retail which keeps compute as fallthrough and zero-block at function end"
next = "try computing zero-branch via explicit goto label placed after compute code, or split into two functions/permuter branch-order search"
score = "85/182"

[[wip]]
name = "sub_08034568"
seed = "src/wip/sub_08034568.c"
notes = "src/wip/sub_08034568.md"
status = "size_mismatch 156/176 (88.6%, compiled 168B vs retail 176B); all field init logic correct (Unk346C0 tail-region zero/const init, 8 byte writes + 3 halfword + several u32); agbcc picks r1-chain instead of r4-chain for final unk2C4 store, dropping one add-chain step vs retail"
next = "try alternate field write order or split into two helper calls matching retail's r1/r4 chain split; permuter candidate (small, near miss)"
score = "156/176"

[[wip]]
name = "sub_08049F98"
seed = "src/wip/sub_08049F98.c"
notes = "src/wip/sub_08049F98.md"
status = "same-size DIFF 36/144 (25%); logic confirmed correct (2x sub_080617C4 header calls, sub_080615EC coord set via idx<<4 chain, sub_08061784 return, sub_0806171C draw); agbcc keeps out-param in r8 across 3 calls in retail (push r8 at entry) but my C only uses out at final call so agbcc doesn't preallocate r8 early — retail's r8 usage implies out is referenced earlier in source or a dummy read keeps it live"
next = "try touching 'out' earlier (e.g. volatile read) or reorder params so out is 2nd arg; also try casting idx shift result through s16 (retail uses lsrs #17 = arithmetic sign truncate to s15 for the return-value cast, suggesting val type may be s16 not u16 -- already tried, needs recheck combined with r8 fix). Twin clones sub_0804E17C/sub_08051578 share same structure - crack this one first, then just re-run match_function.py with same seed"
score = "unscored"

[[wip]]
name = "sub_080385DC"
seed = "src/wip/sub_080385DC.c"
notes = "src/wip/sub_080385DC.md"
status = "same_size DIFF 18/80; shared masks and r12/r6/r1 pointer/register hints did not improve the baseline"
next = "leave readable Thumb; revisit with targeted pointer-cache/permuter technique"
score = "18/80"

[[wip]]
name = "sub_080733E4"
seed = "src/wip/sub_080733E4.c"
notes = "src/wip/sub_080733E4.md"
status = "size_mismatch 14/90 baseline; explicit-goto and r2/r5-pinned variants did not reproduce retail loop allocation"
next = "leave readable Thumb; revisit with permuter over skip/main-loop source shape"
score = "14/90"

[[wip]]
name = "sub_08069B78"
seed = "src/wip/sub_08069B78.c"
notes = "src/wip/sub_08069B78.md"
status = "same-size DIFF 62/154 (40.3%); shared mask/inverse and r10/r5 hints did not improve post-call register scheduling"
next = "leave readable Thumb; revisit with targeted post-call register scheduling or permuter"
score = "62/154"

[[wip]]
name = "sub_0806E7BC"
seed = "src/wip/sub_0806E7BC.c"
notes = "src/wip/sub_0806E7BC.md"
status = "size_mismatch 42/152 (27.6%), compiled 148B vs retail 152B; attempted as line-segment intersection test (2D cross-product sign comparison, 8 s32 params ax,ay,bx,by,cx,cy,dx,dy) returning 0/1/2; logic direction plausible (first 8 bytes match: prologue) but overall structure/expression grouping doesn't match agbcc's register scheduling - needs more careful param/expr-order derivation from raw asm (subs order matters: check if retail computes abx=ax-bx or bx-ax first, etc)"
next = "re-derive param roles more carefully from asm (r12=a0 orig, r6=a1, r5=a2, r4=a3; stack args sp0x18/1C/20/24) - do NOT assume signature guessed here is correct; trace each subs/muls instruction to exact source expr before writing C"
score = "42/152"

[[wip]]
name = "sub_08041858"
seed = "src/wip/sub_08041858.c"
notes = "src/wip/sub_08041858.md"
status = "47/52 size mismatch; direct typed stores also retained agbcc's pool-address fold"
next = "leave readable Thumb; revisit with a distinct pool-symbol technique or shape-preserving call boundary"
score = "47/52"

[[wip]]
name = "sub_08043944"
seed = "src/wip/sub_08043944.c"
notes = "src/wip/sub_08043944.md"
status = "best 77.1% (37/48) same-size DIFF after r3/r4 hints; input-normalization order still differs"
next = "leave readable Thumb; revisit with a source shape that forces idx normalization before flags normalization"
score = "37/48"

[[wip]]
name = "sub_08031300"
seed = "src/wip/sub_08031300.c"
notes = "src/wip/sub_08031300.md"
status = "83.3% (65/78) same-size DIFF; logic correct; decomp-permuter ran about 10,000 iterations and reached best score 1295 without zero"
next = "leave readable Thumb; revisit with a new register-allocation technique or a verified compiler/source-shape insight"
score = "65/78"

[[wip]]
name = "sub_08033084"
seed = "src/wip/sub_08033084.c"
notes = "src/wip/sub_08033084.md"
status = "63/100 same-size DIFF; semantic logic correct, but retail copies the destination into r5 before normalizing the flag while agbcc schedules that copy after lsls"
next = "try a register-parameter or source shape that forces dst=a before flag normalization; keep loc=r4 and the explicit gBattleWork reload in the <=0 branch"
score = "63/100"

[[wip]]
name = "sub_08033574"
seed = "src/wip/sub_08033574.c"
notes = "src/wip/sub_08033574.md"
status = "20/80 after embedded-state modeling; semantic field map is plausible, but agbcc uses r4/r5 and omits retail r6 while the retail prologue keeps the global pointer in r5, base in r1, and 0x2088 in r6"
next = "try explicit r5 global-pointer and r6 offset register pins, while preserving the embedded state at 0x1FAC and byte flag at +0x3A"
score = "20/80"

[[wip]]
name = "sub_08038580"
seed = "src/wip/sub_08038580.c"
notes = "src/wip/sub_08038580.md"
status = "best 24/92 same-size DIFF; logic and size are correct, but baseline uses dst in r6, p in r2, and mask in r7 while retail uses dst r2, p r0, mask r6, and preserves original index in r7; direct all-register pinning worsened to 13/92 and 104B"
next = "try selective pins only: dst r2 and original index r7, leaving global pointer/mask allocation to agbcc; preserve the 92-byte baseline shape"
score = "24/92"

[[wip]]
name = "sub_0803DCFC"
seed = "src/wip/sub_0803DCFC.c"
notes = "src/wip/sub_0803DCFC.md"
status = "11/48 same-size DIFF; index pinning and explicit goto preserved size, but retail uses a longer error-path layout and explicit shift/add sequence while agbcc still places the table return block differently"
next = "try a source layout that keeps the error call block before the valid table path while forcing  index and separate  offset; use the retail bls target shape"
score = "11/48"

[[wip]]
name = "sub_0803E328"
seed = "src/wip/sub_0803E328.c"
notes = "src/wip/sub_0803E328.md"
status = "44/76 same-size DIFF; signed table indices and delayed first-table byte now match the retail shape, but agbcc schedules the 0x0807BDB8 literal load after setting up the 0x1E index"
next = "pin the first table base to r0 or assign it in a separate statement before reading unk1E; preserve the current entry1/table2/table3 evaluation order"
score = "44/76"

[[wip]]
name = "sub_0803E374"
seed = "src/wip/sub_0803E374.c"
notes = "src/wip/sub_0803E374.md"
status = "40/76 same-size DIFF; cloned signed table lookup matches size and most arithmetic, but agbcc still schedules the 0x0807BDB8 literal after the 0x1E index setup despite r0/r1 pins"
next = "try a dependency-preserving table-base load before the first signed index (or reuse the successful 3E328 pattern with a forced r0 table local); the remaining table accesses are offset +1"
score = "40/76"

[[wip]]
name = "sub_0803E3C0"
seed = "src/wip/sub_0803E3C0.c"
notes = "src/wip/sub_0803E3C0.md"
status = "40/76 same-size DIFF; clone table offsets +2 are correct, but agbcc retains the same literal-load scheduling difference seen in 3E328/3E374"
next = "solve the shared three-function table-load ordering pattern once, then apply the byte offset variant to this seed"
score = "40/76"

[[wip]]
name = "sub_08040088"
seed = "src/wip/sub_08040088.c"
notes = "src/wip/sub_08040088.md"
status = "41/56 same-size DIFF after pinning target to r3; retail still keeps the target-pointer location in r1 and the timer field address in r2, while agbcc uses r2/r1 oppositely"
next = "pin  to r1 (and let target remain r3); preserve direct  member access so the initial 0x9D/0xC1 offset setup follows retail"
score = "41/56"

[[wip]]
name = "sub_08040EF4"
seed = "src/wip/sub_08040EF4.c"
notes = "src/wip/sub_08040EF4.md"
status = "best 34/88 size-mismatch after raw offset and full register pins; baseline was 13/88 at 72B, while retail requires r5 key, r4 byte offset, r2/r3 table cursors, and r6 base+4 without the extra saved registers"
next = "try only key r5 and offset r4 pins; let agbcc allocate table cursors, while retaining an explicit second-word base to keep the adds r4+r6 shape"
score = "34/88"

[[wip]]
name = "sub_08042B28"
seed = "src/wip/sub_08042B28.c"
notes = "src/wip/sub_08042B28.md"
status = "35/40 same-size DIFF after pinning index to r2; retail loads the ROM table base into r0 before shifting index, while agbcc computes the shift in r0 and loads base into r1"
next = "pin the table base to r0 and retain the index r2 pin; this should reproduce the remaining literal-load register order"
score = "35/40"

[[wip]]
name = "sub_08042B50"
seed = "src/wip/sub_08042B50.c"
notes = "src/wip/sub_08042B50.md"
status = "35/40 same-size DIFF; clone logic and register pins reproduce the exact size, but agbcc still places the table-base literal after the index shift instead of retail's r0-before-r1 sequence"
next = "reuse the 42B28 selective base/index shape with a dependency that prevents scheduling the index shift before the ROM literal load"
score = "35/40"

[[wip]]
name = "sub_08042B78"
seed = "src/wip/sub_08042B78.c"
notes = "src/wip/sub_08042B78.md"
status = "45/56 same-size DIFF after r1/r2/r3/r4 shaping; loop and sentinel behavior now match, but retail loads the ROM table into r2 before materializing -1 in r1, while agbcc schedules -1 first"
next = "force the base assignment to be live before the minus-one assignment, then preserve the r4 copy and r1 table-cursor reuse"
score = "45/56"

[[wip]]
name = "sub_08042BB0"
seed = "src/wip/sub_08042BB0.c"
notes = "src/wip/sub_08042BB0.md"
status = "53/56 same-size DIFF (94.6%); initial prologue and loop match, but key/value cursors are allocated opposite to retail at the final 4-byte load: retail keeps key cursor in r2 and value cursor in r1"
next = "pin the key cursor/base to r2 and the value cursor to r1 after the sentinel check; return valueEntry[1] and test the sentinel through valueEntry"
score = "53/56"

[[wip]]
name = "sub_08042BE8"
seed = "src/wip/sub_08042BE8.c"
notes = "src/wip/sub_08042BE8.md"
status = "best 37/82 size-mismatch; explicit r4/r5/r6/r7/r3/r2 shaping reproduces the loop body, but agbcc emits push r4-r6 while retail saves r7 as well and retains a longer branch layout"
next = "force r7 as an ordinary live callee-saved local rather than only a fixed register pointer; then preserve the current byte-offset table cursors"
score = "37/82"

[[wip]]
name = "sub_08042C3C"
seed = "src/wip/sub_08042C3C.c"
notes = "src/wip/sub_08042C3C.md"
status = "54/56 same-size DIFF (96.4%); exact prologue, sentinel handling, cursor registers, and return are matched; only the two cursor increments are reversed (retail value cursor r1 then key cursor r2)"
next = "swap the source order of  and ; this is a two-byte instruction-order near-match"
score = "54/56"

[[wip]]
name = "sub_08042F4C"
seed = "src/wip/sub_08042F4C.c"
notes = "src/wip/sub_08042F4C.md"
status = "33/80 size-mismatch; row update and six-argument notification logic are correct, but the s16 parameter c is normalized before sub_08042E78 while retail passes r2 directly; casted call did not alter the caller normalization"
next = "verify whether sub_08042E78's prototype should accept s32 for this caller (without changing its own matching definition), then retry the same source shape"
score = "33/80"

[[wip]]
name = "sub_08043B90"
seed = "src/wip/sub_08043B90.c"
notes = "src/wip/sub_08043B90.md"
status = "67/76 same-size DIFF; sibling-shaped semantic C matches the loop and bytes, but agbcc keeps a two-byte mov/branch placement difference around the shared return path after the ROM literal pool"
next = "use an explicit label/goto layout that preserves r0 from the terminating unk00 load and aligns the return path with the literal-pool gap"
score = "67/76"

[[wip]]
name = "sub_080473F8"
seed = "src/wip/sub_080473F8.c"
notes = "src/wip/sub_080473F8.md"
status = "50/100 bytes (50%); semantically correct but agbcc emits 'subs r1,#8' peephole instead of retail's full literal reload for the second global-address load (gUnk_03000630 = gUnk_03000638 - 8), a 2-byte vs 4-byte instr diff every variant hits"
next = "try forcing literal reload: maybe split into two functions temporarily, or check if retail was compiled with different codegen version/flags for this TU; alternatively try asm-volatile-free trick of loading address via array indexing gUnk_0300063C-style neighbor to shift pool layout"
score = "50/100"

[[wip]]
name = "sub_08052934"
seed = "src/wip/sub_08052934.c"
notes = "src/wip/sub_08052934.md"
status = "55/84 bytes (65.5%), same size; semantically correct 2-level table lookup (0x080995AC+0xC indexed by arg0*16, then by gMainWorkPtr->unk1818) but agbcc folds the '0x080995AC + 0xC' constant into a single 4-byte literal (0x080995B8) instead of retail's split load+adds (ldr =0x080995AC; adds #0xC), a 2-instruction/0-byte-diff but different encoding order"
next = "try declaring 0x080995AC as an extern rom-data symbol/array so +0xC becomes a real field access (relocation) instead of constant-folded arithmetic, which may force the split load"
score = "55/84"

[[wip]]
name = "sub_08059DC8"
seed = "src/wip/sub_08059DC8.c"
notes = "src/wip/sub_08059DC8.md"
status = "27/72 bytes (37.5%); this is a state-machine dispatcher that calls a fixed bx-r4 trampoline (_08073C50 at 0x08073C50: 'bx r4') with the actual handler loaded from a jump table at 0x08099710[list->field0] into r4 right before each bl. agbcc dead-code-eliminates the register(r4)-pinned handler assignment since it's never read in C (only consumed implicitly via the trampoline's bx r4), so the table lookup vanishes from output"
next = "may need inline asm (not just register-pinned var) to force the r4 load to survive codegen immediately before the bl _08073C50 call, or split into two statements bridged by a volatile-style barrier; check other _08073C50 callers (sub_08069270, sub_0806C7D4, sub_0806D748) for the same pattern once one is solved"
score = "27/72"

[[wip]]
name = "sub_08061800"
seed = "src/wip/sub_08061800.c"
notes = "src/wip/sub_08061800.md"
status = "41/76 bytes (53.9%), same size; correctly identified _08073C4C as a 'bx r3' trampoline (4th C arg = handler fn ptr, confirmed by precedent in sub_08035884.c) and the 3rd call arg as the reused 'stride' intermediate (not zero, matching retail leaving r2 unset/leftover). Remaining diff is pure register-allocation/instruction-ordering noise around the p->unk5D and p->unk98 field loads (retail resets r0=p fresh before each offset add; agbcc here does incremental adds/reuses r1 directly for the shift)"
next = "try computing p->unk5D and p->unk98 as two separate top-level statements each assigned through a fresh local rather than inline in the lo/h expressions, or check if declaring 'p' as volatile changes agbcc's incremental-offset optimization"
score = "41/76"

[[wip]]
name = "sub_08061BE8"
seed = "src/wip/sub_08061BE8.c"
notes = "src/wip/sub_08061BE8.md"
status = "64/96 bytes (66.7%), same size; correct logic (index gUnk_03000770 by gUnk_03000794-1, deref entry->unk00 twice through _08073C4C bx-r3 trampoline, then copy entry->unk04/unk06 into gUnk_03000798->unk90/unk92) but agbcc uses one fewer callee-saved register (push {r4,r5,lr} vs retail's {r4,r5,r6,lr}) and reorders the trampoline-arg setup slightly"
next = "try forcing an extra live temp across the _08073C4C call (e.g. cache entry->unk00 deref result in its own named var before the call) to nudge agbcc into allocating r6 like retail"
score = "64/96"

[[wip]]
name = "sub_08061D68"
seed = "src/wip/sub_08061D68.c"
notes = "src/wip/sub_08061D68.md"
status = "13/88 bytes (13.5%), size mismatch (96 vs 88); correct overall algorithm (mask args to 0x1F, build 12-bit palette-select field, swap lo/hi if needed, compute VRAM tile address from gUnk_03000798->unk5C, loop-OR the palette bits into each halfword) but agbcc allocates 4 callee-saved regs (r4-r7) vs retail's 3 (r4-r6) -- retail reuses a non-callee-saved scratch reg (r1) for the swap temp instead of a persistent local"
next = "drop the explicit 'tmp' local and do the swap via a compound/ternary expression, or restructure so the swap temp doesn't need to outlive the swap statement (matching retail's transient r1 use)"
score = "13/88"

[[wip]]
name = "sub_08061DC0"
seed = "src/wip/sub_08061DC0.c"
notes = "src/wip/sub_08061DC0.md"
status = "36/72 bytes (50%), same size; correct algorithm (bx-r3 trampoline _08073C4C called with two VRAM addresses computed from gUnk_03000798->unk5D/unk98 and stride, plus the stride itself as 3rd arg) but agbcc allocates 2 callee-saved regs (r4,r5) vs retail's 1 (r4 alone holds the 0x06000000 constant; everything else stays in r0-r3)"
next = "retail keeps arg1 in r3 (not promoted to a callee-saved reg) through both multiplies -- try computing the second address expression first (using arg1) before the first (arg0), or avoid a separate 'stride'/'lo' local by inlining the field reads directly into the two address expressions"
score = "36/72"

[[wip]]
name = "sub_08062068"
seed = "src/wip/sub_08062068.c"
notes = "src/wip/sub_08062068.md"
status = "16/48 bytes (28.6%), size mismatch (56 vs 48); correct algorithm (3-way select on mode: 0=unk1E-(b>>1), 1=unk1E-b, 2=unk1E, default returns garbage matching retail's fallthrough). A switch(mode){0,2,1} gets the first comparison right (beq mode==1, jumping to a case body placed last) but agbcc emits a 5-compare binary-search-style dispatch instead of retail's compact 3-compare chain (beq 1; bcc(<1 unsigned); beq 2)"
next = "try an if/else-if chain ordered exactly as retail's comparisons (== 1 first, then unsigned < 1, then == 2) but check whether a ternary or goto-based structure forces agbcc to keep the beq-forward-jump shape from the switch attempt without the extra bgt test"
score = "16/48"

[[wip]]
name = "sub_080620D4"
seed = "src/wip/sub_080620D4.c"
notes = "src/wip/sub_080620D4.md"
status = "10/72 bytes (12.5%), size mismatch (80 vs 72); correct algorithm (walk a->unk0C[0..0x7F] entries, shift each entry's unk08/unk0C by dx<<8/dy<<8, and on the first non-null entry snapshot entries[0]->unk08/unk0C into a->unk10/unk14). Discovered/reused existing struct Unk62044 (already matched by sub_08062044.c/sub_0806209C.c) and added struct Unk620D4Entry for the array elements. Retail copies raw dy into r3 immediately after push, before the null checks; agbcc here defers the dy shift and ends up spilling through an 'ip' register (extra mov), needing one more callee-saved register than retail"
next = "try computing shiftedDy as the FIRST statement (before the two null checks) so agbcc materializes it early like retail does with raw dy, or split shiftedDx/shiftedDy computation into the loop body itself rather than hoisting both before the null checks"
score = "10/72"

[[wip]]
name = "sub_08062A74"
seed = "src/wip/sub_08062A74.c"
notes = "src/wip/sub_08062A74.md"
status = "53/76 bytes (69.7%), same size; correct algorithm and instruction shape matches retail almost exactly (table-address kept in r6, dereferenced fresh both before the trampoline call and again before the final store) after discovering/adding struct Unk62A74 (16-slot pool + u16 used-bitmask at *gUnk_030008D0, shared with sub_08062AF8/sub_08062B9C). Only remaining diff: agbcc allocates r2 for the dereferenced slot pointer where retail uses r1"
next = "try declaring 'slot' after 'idx' or forcing a compound expression for the null check to see if it shifts register choice from r2 to r1"
score = "53/76"

[[wip]]
name = "sub_08062B9C"
seed = "src/wip/sub_08062B9C.c"
notes = "src/wip/sub_08062B9C.md"
status = "34/76 bytes (44.7%), same size; correct algorithm (clear bitmask bit + slot pointer for idx in [lo,hi] after swap, reusing struct Unk62A74 from sub_08062A74) with the full loop/branch shape matching retail (confirmed via manual disassembly comparison -- only the register chosen for arg1/hi differs: retail keeps it in r4 from function entry, agbcc here picks r5)"
next = "try passing arg1 as the first declared local or via a temp assigned before arg0's, or check if reversing argument order in a wrapper affects register choice"
score = "34/76"
