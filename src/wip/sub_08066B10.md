# sub_08066B10 — WIP

| | |
|--|--|
| ROM | `0x08066B10` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08066B10.c` |
| Last `match_function.py` | 18/180 then 91/180
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two attempts: u32 argument plus fixed registers 18/180 (192B), then u8 parameter with ordinary result 91/180 (same-size). Semantic cases, toggle, string handling, and offset calculation are mapped; remaining mismatch is only the entry register/source ordering (retail copies a to r5 before masking index into r4) and the result/r7 allocation shape in the first seed.

## Current state

Two attempts: u32 argument plus fixed registers 18/180 (192B), then u8 parameter with ordinary result 91/180 (same-size). Semantic cases, toggle, string handling, and offset calculation are mapped; remaining mismatch is only the entry register/source ordering (retail copies a to r5 before masking index into r4) and the result/r7 allocation shape in the first seed.

## Next

Use a u32 index_arg with a local index pinned to r4 and state pinned to r5; assign state before index. Keep result ordinary so agbcc saves/uses retail r7, with length as the remaining temporary.
