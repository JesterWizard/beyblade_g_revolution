# sub_08044F64 — WIP

| | |
|--|--|
| ROM | `0x08044F64` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08044F64.c` |
| Last `match_function.py` | 69/74
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: 93.2% (69/74) same-size DIFF; retail computes idx*3 as muls r0,r1 (result stays in r0/param reg) then adds r5,r0,#0 (copy to loop var); my C always emits adds r5,r0,#0 then muls r5,r1 regardless of statement order tried

## Current state

93.2% (69/74) same-size DIFF; retail computes idx*3 as muls r0,r1 (result stays in r0/param reg) then adds r5,r0,#0 (copy to loop var); my C always emits adds r5,r0,#0 then muls r5,r1 regardless of statement order tried

## Next

try idx as the loop variable directly (no separate i), or a macro/inline asm just for the multiply; logic and struct usage confirmed correct
