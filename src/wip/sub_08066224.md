# sub_08066224 — WIP

| | |
|--|--|
| ROM | `0x08066224` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08066224.c` |
| Last `match_function.py` | 63/112 then 61/112
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two attempts: direct typed semantic C 63/112 (116B), then register anchors 61/112 (same-size). Bounds and state updates are mapped; remaining mismatch is initial current-entry register/source ordering and the callback index return-width shape (retail directly shifts sub_08072F94 result by 2, candidate zero-extends the u8 return).

## Current state

Two attempts: direct typed semantic C 63/112 (116B), then register anchors 61/112 (same-size). Bounds and state updates are mapped; remaining mismatch is initial current-entry register/source ordering and the callback index return-width shape (retail directly shifts sub_08072F94 result by 2, candidate zero-extends the u8 return).

## Next

Restore `current = a->unk28 + a->unk04` before any base assignment, remove the flags temporary and use the direct bit test, then form `entry = a->unk28 + index`. Keep current r3/a r6/state r5/entry r4 as useful anchors. Try a u32 slot temporary or direct pointer arithmetic to avoid the u8 zero-extension before shifting.
