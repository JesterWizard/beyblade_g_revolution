# sub_080442FC — WIP

| | |
|--|--|
| ROM | `0x080442FC` |
| Retail | `adds r5,r0` (a); `movs r4,#0` (index) |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080442FC.c` |
| Last `match_function.py` | 132/144 same-size |
| Sibling / types | `Unk442FC` / `MainWork` unk1690 unk181B |

## Role

Resolve a table row from unk181B, store it at unk1690, call `sub_08043C70`.

## Process

- `index = 0` first still copies `a` to r4. Need a in r5, index in r4.
- Permuter not run long this session.

## Current state

132/144. `a`/index swapped r4/r5.

## Next

`perm_refer_to_var` so r4 is taken before `a` is copied.
