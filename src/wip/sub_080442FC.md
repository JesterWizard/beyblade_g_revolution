# sub_080442FC — WIP

| | |
|--|--|
| ROM | `0x080442FC` |
| Retail | `adds r5,r0` (a); `movs r4,#0` (index) |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080442FC.c` |
| Last `match_function.py` | 132/144 same-size |
| Sibling / types | `Unk442FC` / `MainWork` unk1690 unk181B |

## Role

Resolve a table row from unk181B, store it at unk1690, call `sub_08043C70`.

## Process

- `index = 0` first still copies `a` to r4. Need a in r5, index in r4.
- Permuter not run long this session.

## Current state

132/144. `a`/index swapped r4/r5.

## Next

`perm_refer_to_var` so r4 is taken before `a` is copied.

## 2026-09-21 — symbols + clean rewrite

Seed raised to **134/144** by addressing `0x08091208` / `0x08091204` through
`gData_08091208` / `gData_08091204` (`asm/data_symbols.s`) instead of raw literals — this stops
agbcc from folding the two pool entries (same trick as `sub_08041858` / `sub_08037318`).
Remaining diff is only the `a`/`index` swap: retail `adds r5,r0` + `movs r4,#0`, agbcc
`adds r4,r0` + `movs r5,#0` (13 halfwords, first at +0x4).

A clean semantic rewrite (`u32 index = 0;` + direct `mw`/`p` locals, no raw temps) is *worse*
— 116/144 — but still copies `a` to r4, so the swap is independent of the seed's temp layout.

`index` has more references than `a`, so retail's global-alloc priority puts it in r4 first;
agbcc apparently reaches the same decision for `a` regardless of ref count / declaration order.
`old_agbcc` → 10/144. Permuter needed.
