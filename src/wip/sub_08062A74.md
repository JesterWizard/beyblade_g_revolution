# sub_08062A74 — WIP

| | |
|--|--|
| ROM | `0x08062A74` |
| Retail | `ldrh r2,[r1]; orrs r0,r2` then `ldr r2,=0x080BB8C0` |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08062A74.c` |
| Last `match_function.py` | 72/76 same-size |
| Sibling / types | `Unk62A74` |

## Role

Mark a palette slot used and CpuSet 0x20 bytes into OBJ PAL + idx*32.

## Process

- `u32 a` so src copies to r5 before the u8 extend. DIFF is ldrh/handler in r3 vs r2.
- Extra dummy `ldrh` made it worse. Permuter 240s stayed at 20.

## Current state

72/76. flags/handler register r3 vs r2.

## Next

Overnight permuter; dummy reload like `sub_0803531C` if it does not add a real insn.
