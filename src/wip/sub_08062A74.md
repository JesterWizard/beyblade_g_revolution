# sub_08062A74 — WIP

| | |
|--|--|
| ROM | `0x08062A74` |
| Retail | `ldrh r2,[r1]; orrs r0,r2` then `ldr r2,=0x080BB8C0` |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08062A74.c` |
| Last `match_function.py` | 72/76 same-size |
| Sibling / types | `Unk62A74` |

## Role

Mark a palette slot used and CpuSet 0x20 bytes into OBJ PAL + idx*32.

## Process

- `u32 a` so src copies to r5 before the u8 extend.
- The best permuter seed reaches 74/76; only the shift/add register pair differs.
- A handler-local rewrite regressed to 64/76; keep the current seed.

## Current state

74/76. The handler pool register is correct, but the shift uses r3 where retail uses r1.

## Next

Try a constrained source/permuter search for `lsls r1,r4,#5` while retaining the handler in r2/r3; do not use register asm.
