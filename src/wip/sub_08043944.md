# sub_08043944 — WIP

| | |
|--|--|
| ROM | `0x08043944` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08043944.c` |
| Last `match_function.py` | 34/48
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: best 70.8% (34/48) same-size DIFF; retail keeps idx in r3 and flags in r4 with a single push{r4,lr}, moves flags->r0 before ldrb into r3, orrs r0,r3; my C either produces push{r4,r5,lr} (idx/flags both spilled) or reorders orr operands wrong

## Current state

best 70.8% (34/48) same-size DIFF; retail keeps idx in r3 and flags in r4 with a single push{r4,lr}, moves flags->r0 before ldrb into r3, orrs r0,r3; my C either produces push{r4,r5,lr} (idx/flags both spilled) or reorders orr operands wrong

## Next

try passing idx/flags packed differently, or write as register-hinted asm() for just the two locals; or try u8 val=(u8)(flags|p->unk08[idx]) with idx read via local copy first
