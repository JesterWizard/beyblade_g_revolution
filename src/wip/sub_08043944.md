# sub_08043944 — WIP

| | |
|--|--|
| ROM | `0x08043944` |
| Retail | |
| `src/matched` | **semantic C** (`match-compiler: old_agbcc`) |
| Seed | `src/wip/sub_08043944.c` |
| Last `match_function.py` | 48/48 — MATCH (old_agbcc) |
| Sibling / types | |

## Role

OR a flag into `gUnk_03000554->unk08[idx]` and mirror the result in `unk02`.

## Process

- 2026-09-20 — parked. Status: best 70.8% (34/48) same-size DIFF; retail keeps idx in r3 and flags in r4 with a single push{r4,lr}, moves flags->r0 before ldrb into r3, orrs r0,r3; my C either produces push{r4,r5,lr} (idx/flags both spilled) or reorders orr operands wrong
- 2026-09-20 — reduced register hints to `idx`→`r3` and `flags`→`r4`;
  reached 37/48 with the right size, but input-normalization order still
  differs. No matched-file changes.
- 2026-09-21 — **matched**. The parked seed is byte-exact when compiled with `old_agbcc`; `agbcc` picks a different register for the load it coalesces into the consuming instruction. Landed in `src/matched/` with `/* match-compiler: old_agbcc */`.

## Current state

matched — semantic C in `src/matched/` (100%, `old_agbcc`).

## Next

done.
