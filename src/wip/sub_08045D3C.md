# sub_08045D3C — WIP

| | |
|--|--|
| ROM | `0x08045D3C` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08045D3C.c` |
| Last `match_function.py` | 43/436 then 81/436
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two attempts: readable semantic seed 43/436 (332B), then switch plus r2/r4/r3/r5 register pins 81/436 (364B). Packed low-byte group/bit semantics, global 8-word bit operations, table save/restore cases 0x3EA/0x3EB, and pointer save/restore cases 0x3EC/0x3ED are documented; initial mask temporary and branch/copy layout still differ.

## Current state

Two attempts: readable semantic seed 43/436 (332B), then switch plus r2/r4/r3/r5 register pins 81/436 (364B). Packed low-byte group/bit semantics, global 8-word bit operations, table save/restore cases 0x3EA/0x3EB, and pointer save/restore cases 0x3EC/0x3ED are documented; initial mask temporary and branch/copy layout still differ.

## Next

Restore pinned switch semantics selectively: retain operation r3/output r5/group r2/bit r4, but force the 0xF8 mask through ordinary r1 (not bit's fixed r4); use a permuter for case dispatch and copy branches.
