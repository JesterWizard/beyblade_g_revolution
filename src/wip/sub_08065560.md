# sub_08065560 — WIP

| | |
|--|--|
| ROM | `0x08065560` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08065560.c` |
| Last `match_function.py` | 58/120 then 66/120
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two attempts: semantic switch/loop 58/120 (116B), then r2/r1/r3/r4/r5 register anchors 66/120 (same-size). Field selection and three writes match; remaining differences are the loop’s initial for-condition (retail enters directly and checks only after decrement) and the retail cursor copy: after computing table base, retail copies selected ROM pointer into r2 and uses r1 only as the byte scratch.

## Current state

Two attempts: semantic switch/loop 58/120 (116B), then r2/r1/r3/r4/r5 register anchors 66/120 (same-size). Field selection and three writes match; remaining differences are the loop’s initial for-condition (retail enters directly and checks only after decrement) and the retail cursor copy: after computing table base, retail copies selected ROM pointer into r2 and uses r1 only as the byte scratch.

## Next

Change the for loop to do-while with i initialized to 2. Leave selected indices as an ordinary pointer so agbcc can copy it into r2 after table-base calculation; keep data initially r2, table r4, source r3, and counter r5.
