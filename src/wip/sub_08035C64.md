# sub_08035C64 — WIP

| | |
|--|--|
| ROM | `0x08035C64` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08035C64.c` |
| Last `match_function.py` | 85/182
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: same_size DIFF 85/182 (46.7%); logic fully correct (repulsion physics on Unk346C0Inner, dx/dy/distSq/threshold gate) but agbcc always emits the early-return (zero) block first regardless of if/else source order — branch layout inverted vs retail which keeps compute as fallthrough and zero-block at function end

## Current state

same_size DIFF 85/182 (46.7%); logic fully correct (repulsion physics on Unk346C0Inner, dx/dy/distSq/threshold gate) but agbcc always emits the early-return (zero) block first regardless of if/else source order — branch layout inverted vs retail which keeps compute as fallthrough and zero-block at function end

## Next

try computing zero-branch via explicit goto label placed after compute code, or split into two functions/permuter branch-order search

- 2026-09-20 — seeded attempt (manual, no permuter): struct field fix
  first — `include/unknown-types.h` `struct Unk346C0Inner` had unk2C
  and unk38 buried in filler ranges (filler_24[0xC], filler_38[4]);
  split them out as named `s32 unk2C`/`s32 unk38` fields (this WIP's
  source now compiles again against current headers — it had bitrotted
  since a previous session's struct edit).
  Tried 4 source-level rephrasings of the if/else, all compiling to
  byte-identical output (84/182, same_size DIFF):
    1. original nested if/sumSq>distSq{ if(unk14<threshold){zero;return0}
       else{compute} }
    2. early-return sumSq<=distSq;  if(unk14>=threshold) goto zero; compute;
       return1; zero: ...; return0;
    3. same as 2 but plain if/else instead of goto
    4. `if (sumSq>distSq && unk14>=threshold) {zero;return0}` then
       `if (sumSq>distSq) {compute}` return1;
  All 4 produce the exact same instruction layout: agbcc places the
  4-store zero block as the fallthrough target and the larger compute
  block behind a forward branch, regardless of C-level if/else order,
  goto placement, or condition polarity. This confirms the layout is
  agbcc's own heuristic (likely block-size/statement-count driven) and
  not reachable from plain C restructuring — needs a permuter run with
  PERM_IGNORE-wrapped raw block reordering or PERM_GENERAL swapping
  the whole if/else as literal text at the AST level (not just
  polarity flips, which agbcc canonicalizes away). Permuter candidate,
  logic fully verified correct, only layout differs.
