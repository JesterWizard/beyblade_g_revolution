# sub_08040EF4 — WIP

| | |
|--|--|
| ROM | `0x08040EF4` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08040EF4.c` |
| Last `match_function.py` | 34/88
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: best 34/88 size-mismatch after raw offset and full register pins; baseline was 13/88 at 72B, while retail requires r5 key, r4 byte offset, r2/r3 table cursors, and r6 base+4 without the extra saved registers

## Current state

best 34/88 size-mismatch after raw offset and full register pins; baseline was 13/88 at 72B, while retail requires r5 key, r4 byte offset, r2/r3 table cursors, and r6 base+4 without the extra saved registers

## Next

try only key r5 and offset r4 pins; let agbcc allocate table cursors, while retaining an explicit second-word base to keep the adds r4+r6 shape
