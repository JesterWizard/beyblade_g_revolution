# sub_080733E4 — WIP

| | |
|--|--|
| ROM | `0x080733E4` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080733E4.c` |
| Last `match_function.py` | unscored
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: size_mismatch 14/90 (15.6%), compiled 88B vs retail 90B; logic likely correct (string copy skipping \n, pads with NUL at end, returns final index-1; early NULL-checks return -1) but overall instruction ordering/branch layout diverges heavily from agbcc -- for(;;i++) with if/else produces different control flow than retail's label-based skip-loop + separate main loop
- 2026-09-20 — tried an explicit-goto block layout, then pinned the
  retail `r2` index/`r5` limit roles; the best candidate was still
  size-mismatched (22/90, 84 bytes). No matched-file changes.

## Current state

size_mismatch 14/90 (15.6%); explicit-goto and r2/r5-pinned variants
did not reproduce the retail skip/main-loop allocation.

## Next

leave readable Thumb; revisit with a permuter search over the skip/main-loop
source shape
