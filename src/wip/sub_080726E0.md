# sub_080726E0 — WIP

| | |
|--|--|
| ROM | `0x080726E0` |
| Retail | `ldrh r4,[r4,#4]` clobbers the object with unk04 |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080726E0.c` |
| Last `match_function.py` | 50/52 same-size |
| Sibling / types | `Unk726E0` |

## Role

CpuSet one stride row from `unk0C` into `dst`.

## Process

- Saving `width = a->unk06` before `unk04` is required so `a` can die.
- Remaining DIFF is `ldrh r1,[r4,#4]` vs `ldrh r4,[r4,#4]`. `obj = *(u16 *)(obj+4)` still allocated dest to r1.

## Current state

50/52 same-size. Last halfword dest register.

## Next

Permuter `perm_refer_to_var` / keep r1 live across that load so dest is r4.
