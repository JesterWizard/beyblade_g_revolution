# sub_080726E0 — WIP

| | |
|--|--|
| ROM | `0x080726E0` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080726E0.c` |
| Last `match_function.py` | 50/52
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: 96.2% (50/52) same-size DIFF; retail clobbers r4 (struct ptr, dead after last field read) to hold a->unk04 before shifting into r1, my C keeps it in a fresh register (r1 directly)
- 2026-09-20 — tried the documented `r4` register hint and a short decomp-permuter
  run; neither improved the 50/52 result. No source or matched-file changes.

## Current state

96.2% (50/52) same-size DIFF; retail clobbers r4 (struct ptr, dead after last field read) to hold a->unk04 before shifting into r1, my C keeps it in a fresh register (r1 directly). The `r4` register hint and a short
decomp-permuter run did not improve the score.

## Next

try assigning a = (void*)a->unk04 style reuse, or pass idx*size via a helper var computed via pointer arithmetic that forces reuse of the a-register; close, revisit with register hint (register u32 r4 asm)
