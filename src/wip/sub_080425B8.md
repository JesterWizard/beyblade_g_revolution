# sub_080425B8 — WIP

| | |
|--|--|
| ROM | `0x080425B8` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080425B8.c` |
| Last `match_function.py` | 108/120 then 27/120
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two attempts: direct sibling seed 108/120 (same-size), then block-scoped r0 main pin worsened to 27/120 (116B). Baseline correctly captures all behavior; retail initial main/offset and mask register ordering need a selective source/permuter search.

## Current state

Two attempts: direct sibling seed 108/120 (same-size), then block-scoped r0 main pin worsened to 27/120 (116B). Baseline correctly captures all behavior; retail initial main/offset and mask register ordering need a selective source/permuter search.

## Next

Restore direct baseline; try only an expression/temporary that produces retail r0 main, r1 +0x479 without pinning the whole function, then preserve corrected +0x448 callback and repeated ring index loads.
