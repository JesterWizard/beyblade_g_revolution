# sub_08061BE8 — WIP

| | |
|--|--|
| ROM | `0x08061BE8` |
| Retail | `ldr r1,=0x03000770; lsls r0,r0,#3; adds r5,r0,r1` |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08061BE8.c` |
| Last `match_function.py` | 92/96 same-size |
| Sibling / types | `Unk0770` / `Unk0798` |

## Role

Apply the last slot in the 0x03000770 table via CpuSet, then copy unk04/unk06 into the sprite.

## Process

- C already has `table = …; n <<= 3; entry = n + table`. agbcc still `lsls` then `ldr`.
- `&n` / `&table` either DCE or +4B. Permuter 240s no improvement.

## Current state

92/96. Two insns swapped (table load vs `lsls #3`).

## Next

Overnight permuter; clone of `sub_08062CC8` `&local` if a second op on `n` can host the table load.
