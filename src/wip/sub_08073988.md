# sub_08073988 — WIP

| | |
|--|--|
| ROM | `0x08073988` |
| Retail | `adds r7,r1` (base); `ldr r0,=0x080BB748; adds r0,r1,r0` |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08073988.c` |
| Last `match_function.py` | 94/96 same-size |
| Sibling / types | glyph width table 0x080BB748 |

## Role

Walk a string and accumulate `delta - table[ch]` (with space/control skips).

## Process

- Plain copies put base in `ip` (`mov r12,r1`) vs r7. `r0 = (index = 0x080BB748)` gets base into r7 but table into r2 (`ldr r2` vs `ldr r0`).
- Permuter from that seed: stuck at score 10 (this 2-byte DIFF) after 2000+ iters.

## Current state

94/96. Table load dest r2 vs r0; base is r7.

## Next

Keep r7 pressure without assigning the table to `index` so `ldr r0,=0x080BB748` remains.
