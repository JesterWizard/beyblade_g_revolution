# sub_080385DC — WIP

| | |
|--|--|
| ROM | `0x080385DC` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080385DC.c` |
| Last `match_function.py` | unscored
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: same_size DIFF 18/80 (20.5%), compiled 88B vs retail 80B; logic correct (clears unk20 bit i, ORs 0xFFFF into unk00[i] and unk22[i] for lo..hi); agbcc re-dereferences gUnk_030003CC pointer macro 3x per loop iter instead of caching once like retail (extra ldr r4,[..] each access); caching into local struct ptr var makes it WORSE (100B, forces r12 spill since r4-r7 all in use)

## Current state

same_size DIFF 18/80 (20.5%), compiled 88B vs retail 80B; logic correct (clears unk20 bit i, ORs 0xFFFF into unk00[i] and unk22[i] for lo..hi); agbcc re-dereferences gUnk_030003CC pointer macro 3x per loop iter instead of caching once like retail (extra ldr r4,[..] each access); caching into local struct ptr var makes it WORSE (100B, forces r12 spill since r4-r7 all in use)

## Next

try passing pointer as a function-local register-hint or restructure to compute all three offsets from one base read per iteration via a single helper macro/temp; or try reordering field writes (unk22 before unk00) to see if agbcc's scheduler picks different caching
