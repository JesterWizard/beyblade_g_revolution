# sub_080385DC — WIP

| | |
|--|--|
| ROM | `0x080385DC` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080385DC.c` |
| Last `match_function.py` | unscored
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: same_size DIFF 18/80 (20.5%), compiled 88B vs retail 80B; logic correct (clears unk20 bit i, ORs 0xFFFF into unk00[i] and unk22[i] for lo..hi); agbcc re-dereferences gUnk_030003CC pointer macro 3x per loop iter instead of caching once like retail (extra ldr r4,[..] each access); caching into local struct ptr var makes it WORSE (100B, forces r12 spill since r4-r7 all in use)
- 2026-09-20 — tried shared `one`/`full` locals and pinned `r12`/`r6`/
  `r1` register roles; neither improved the 18/80 baseline. No
  matched-file changes.

## Current state

same_size DIFF 18/80 (20.5%); logic correct, but shared-mask locals
and pinned `r12`/`r6`/`r1` roles did not reproduce the retail pointer
cache/register shape.

## Next

leave readable Thumb; revisit with a targeted pointer-cache/permuter
technique
