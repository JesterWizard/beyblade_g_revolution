# sub_08034A68 — WIP

| | |
|--|--|
| ROM | `0x08034A68` |
| Retail | `0xC4<<2` / `0xC5<<2` then `subs #0x1C` for unk2F8; status `ldrb r1`; zero in r3 |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08034A68.c` |
| Last `match_function.py` | 91/112 same-size |
| Sibling / types | `Unk346C0` |

## Role

Clear animation status flags and optionally run the 0x3C unk2F8 path.

## Process

- Offset walk (`0xC4<<2`, `0xC5<<2`, `subs #0x1C`) is 91/112; first offset lands in r0 not r1 (`movs r0,#0xC4` vs `movs r1,#0xC4`).
- Permuter 180s: 130 → 120. Need first `off` in r1 so `adds r0, r4, r1`.

## Current state

91/112 same-size. Offset in r0 vs r1.

## Next

Force `off` into r1 (occupy r0 after the `a`/`b` copies). Permuter `perm_refer_to_var`.
