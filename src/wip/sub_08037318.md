# sub_08037318 — WIP

| | |
|--|--|
| ROM | `0x08037318` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08037318.c` |
| Last `match_function.py` | 91/100
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: two attempts: 91/100 same-size; only the ROM-table literal load is scheduled after the stride multiply instead of before it, with the remaining instructions matching

## Current state

two attempts: 91/100 same-size; only the ROM-table literal load is scheduled after the stride multiply instead of before it, with the remaining instructions matching

## Next

Force the 0x030002A0 base load before the index*0x2C calculation using a targeted permuter/source-order search; preserve the exact 8-argument 6FF58 call
