# sub_08037318 — WIP

| | |
|--|--|
| ROM | `0x08037318` |
| Retail | `ldr r1,=0x030002A0; movs r0,#0x2C; … muls; adds r5,r5,r1` |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08037318.c` |
| Last `match_function.py` | 91/100 same-size |
| Sibling / types | `Unk002A0Record` stride 0x2C; `Unk346C0.unk28C` |

## Role

Alloc a sprite object from IWRAM record `index`.

## Process

- C has table then `0x2C`. agbcc emits `movs r0,#0x2C` then the table `ldr`.
- `&r1` / index spill DCE. Permuter 180s no improvement (base 205).

## Current state

91/100. Table load vs `movs #0x2C` order.

## Next

`sub_08062CC8`-style `&local` on the index with the table load between two uses; overnight permuter.
