# sub_080632F8 — WIP

| | |
|--|--|
| ROM | `0x080632F8` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080632F8.c` |
| Last `match_function.py` | 148/184 then 48/184
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: stack-struct loop 148/184 same-size, then register-pinned initialization 48/184 (188B). The local layout and switch/callback semantics match; remaining differences are loop form (retail is a do-while with the done check only at the bottom) and initial palette load ordering (retail loads the BB8BC literal into r0, sets size, then loads the palette value into r3).

## Current state

Two semantic attempts: stack-struct loop 148/184 same-size, then register-pinned initialization 48/184 (188B). The local layout and switch/callback semantics match; remaining differences are loop form (retail is a do-while with the done check only at the bottom) and initial palette load ordering (retail loads the BB8BC literal into r0, sets size, then loads the palette value into r3).

## Next

Use a do-while loop and replace the r3 palette value pin with a palette pointer pinned to r0, assigned before the 0x328 size expression; pass the dereferenced palette pointer to the initial copy.
