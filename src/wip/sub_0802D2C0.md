# sub_0802D2C0 — WIP

| | |
|--|--|
| ROM | `0x0802D2C0` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0802D2C0.c` |
| Last `match_function.py` | 92/108
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: DIFF, best clean rewrite 92/108 bytes; agbcc DCEs the reachable-but-redundant else-if that retail keeps
- 2026-09-20 — verified the retail branch directions directly: the table
  call is for `unk15D0 > unk15D2`, while the zero call is for `<`. Updated
  the WIP seed to preserve those semantics. A reordered table-base candidate
  still compiled to 46/108 and 104 bytes, so no matched-file changes.

## Current state

DIFF, best clean rewrite 92/108 bytes; branch semantics are now corrected
(`>` table call, `<` zero call), but the current compiler shape is 46/108
and 104 bytes because the retail literal-pool/register layout differs.

## Next

permuter run targeting the corrected branch shape and literal-pool/register
order
