# sub_0806A314 — WIP

| | |
|--|--|
| ROM | `0x0806A314` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0806A314.c` |
| Last `match_function.py` | 38/144 then 40/144
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: direct allocator/linked-list wrapper 38/144 (same size), then register-shaped pointer/marker rewrite 40/144 (140B). The allocator and node sentinel semantics are mapped; the remaining prologue issue is loading the 0x03003F40 literal after reading *0x03003F44, while the candidate loads it first, plus counter literal-pool and post-node register order.

## Current state

Two semantic attempts: direct allocator/linked-list wrapper 38/144 (same size), then register-shaped pointer/marker rewrite 40/144 (140B). The allocator and node sentinel semantics are mapped; the remaining prologue issue is loading the 0x03003F40 literal after reading *0x03003F44, while the candidate loads it first, plus counter literal-pool and post-node register order.

## Next

Introduce a current_value local assigned from *0x03003F44 before assigning the 0x03003F40 pool address to r2; branch on current_value, use it to zero 0x03003F48/4C, and preserve separate literals for both counters. Then tune payload r0/next_payload r1/marker r2 around the two sentinel stores.
