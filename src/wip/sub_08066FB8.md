# sub_08066FB8 — WIP

| | |
|--|--|
| ROM | `0x08066FB8` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08066FB8.c` |
| Last `match_function.py` | 101/300 then 93/300
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: direct renderer 101/300 (304B), then r4/r5/r6 register/table anchors 93/300 (292B). Call sequences and mode branches match; remaining same-size scheduling is the table/index literal loads, which retail performs after the first sub_080615EC and represents as 0x080BB110 plus a runtime +0x0C, while the candidate loads 0x080BB11C before the call.

## Current state

Two semantic attempts: direct renderer 101/300 (304B), then r4/r5/r6 register/table anchors 93/300 (292B). Call sequences and mode branches match; remaining same-size scheduling is the table/index literal loads, which retail performs after the first sub_080615EC and represents as 0x080BB110 plus a runtime +0x0C, while the candidate loads 0x080BB11C before the call.

## Next

Move table/index_loc initialization after the first sub_080615EC(0, 8), assign table from 0x080BB110 and perform table += 3 as a separate post-call statement to preserve the retail literal/add sequence. Keep table r5, index_loc r6, image r4.
