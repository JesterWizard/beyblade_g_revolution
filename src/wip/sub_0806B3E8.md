# sub_0806B3E8 — WIP

| | |
|--|--|
| ROM | `0x0806B3E8` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0806B3E8.c` |
| Last `match_function.py` | 61/84
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: 61/84 bytes (72.6%), same size; correct algorithm (walk arg0->unk10 string char-by-char, for each non-space non-null char look up a byte in table 0x080BB748, call sub_0806833C on the current item, set item->unk70=-1, advance item by 0xDC and decrement count; after the string ends, zero-fill unk70 for any remaining items). Added structs Unk6B3E8/Unk6B3E8Item (0xDC stride). Only remaining diff: register choice for the loop char variable 'c' (r0 vs retail's r1)

## Current state

61/84 bytes (72.6%), same size; correct algorithm (walk arg0->unk10 string char-by-char, for each non-space non-null char look up a byte in table 0x080BB748, call sub_0806833C on the current item, set item->unk70=-1, advance item by 0xDC and decrement count; after the string ends, zero-fill unk70 for any remaining items). Added structs Unk6B3E8/Unk6B3E8Item (0xDC stride). Only remaining diff: register choice for the loop char variable 'c' (r0 vs retail's r1)

## Next

try declaring 'c' as a function parameter-style register variable, or check if the initial 'goto check' idiom (vs a for/while) changes register allocation for the char
