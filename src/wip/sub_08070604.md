# sub_08070604 — WIP

| | |
|--|--|
| ROM | `0x08070604` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08070604.c` |
| Last `match_function.py` | 86/92 (93.5%)
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: size_mismatch 82/92 (85.4%), compiled 96B vs retail 92B; all field init logic + s16<<8 fixed-point cast for x/y confirmed correct (struct Unk70604 fully init'd); only remaining diff is tail: retail computes &dst->unk29 into r0 once then reuses r0+1 for &dst->unk2A store, my C recomputes ip+0x2a fresh (4 extra bytes, mov+adds+mov+adds vs adds r0,#1)

- 2026-09-20 — seeded permuter run (`tools/decomp/permuter/permute.sh
  bg nonmatchings/sub_08070604 -j 6 --stop-on-zero`), seeded with
  `u8 *p = &dst->unk29;` + `PERM_GENERAL` on 3 pointer-reuse phrasings
  (increment-then-store / store-then-increment / p+1 no-increment)
  wrapped in `PERM_RANDOMIZE`. Ran ~30k iterations over ~15 min,
  plateaued at score 30 (best candidate used an
  `unsigned long long new_var = src->unk05;` intermediate before the
  `p += 1; *p = new_var + 4;` store — a 64-bit-promoted temp, not a
  u8/u16/u32 one). Manually reproduced and refined this in real headers:
  `unsigned long long tmp; *p = 0; tmp = src->unk05; p += 1;
  *(s8 *)p = tmp + 4;` scores 86/92 (93.5%), best result so far (up
  from 82/92). Tried u8/s8/u16/u32/s32/long/unsigned long for tmp's
  type — only `long long`/`unsigned long long` reach 93.5%, everything
  else regresses to 74/92 (80.4%). Also tried reordering
  tmp-assignment vs `*p=0`/`p+=1` (5 permutations) and `4+tmp` vs
  `tmp+4` — no improvement over the 86/92 result.

## Current state

86/92 bytes matched (93.5%), same_size DIFF. Remaining diff (per
objdump byte-diff) is just the final `ldrb + adds + adds + strb`
tail: retail computes `src->unk05 + 4` into r1 BEFORE incrementing
the pointer to r0 (`ldrb r1,[r1,#5]; adds r1,#4; adds r0,#1;
strb r1,[r0]`); our best compiles to `ldrb r2,[r1,#5]; adds r0,#1;
adds r2,#4; strb r2,[r0]` — same byte count, different
instruction/register order. This is very likely an
agbcc scheduling quirk tied to the `unsigned long long` promotion
of the byte load, not reachable by further pure-C reordering (5
variants tried, all either match this 86/92 plateau or regress).

## Next

Permuter seed should target the exact remaining divergence: PERM_VAR
or PERM_GENERAL specifically on the *order* of the `tmp = src->unk05`
read vs the `p += 1` increment, restricted to swapping only those two
statements (not the whole tail), plus try seeding with `long long`
(signed) explicitly since unsigned vs signed 64-bit both scored
identically — maybe a `PERM_GENERAL(p += 1;, p++;, ++p;)` phrasing
difference nudges agbcc's scheduler. Very close; permuter candidate.
