# sub_080353A0 — WIP

| | |
|--|--|
| ROM | `0x080353A0` |
| Retail | |
| `src/matched` | **semantic C** (`match-compiler: old_agbcc`) |
| Seed | `src/wip/sub_080353A0.c` |
| Last `match_function.py` | 200/200 — MATCH (old_agbcc) |
| Sibling / types | |

## Role

Dispatch the three sub-objects at `a->unk1C`/`a->unk1D4`/`a->unkF8` through `sub_080686D8` + `sub_08035468` according to the flags in `a->unk2C5`.

## Process

- 2026-09-20 — parked. Status: two attempts: 105/200 (208B) direct draft, then 105/200 (204B) after correcting the third angle source to parent inner->unk52; register/order and first mask scheduling still differ
- 2026-09-21 — **matched**. The parked seed is byte-exact when compiled with `old_agbcc`; `agbcc` picks a different register for the load it coalesces into the consuming instruction. Landed in `src/matched/` with `/* match-compiler: old_agbcc */`.

## Current state

matched — semantic C in `src/matched/` (100%, `old_agbcc`).

## Next

done.
