# sub_08069B78 — WIP

| | |
|--|--|
| ROM | `0x08069B78` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08069B78.c` |
| Last `match_function.py` | 62/154
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: size_mismatch 62/154 (40.3%), compiled 152B vs retail 154B; logic correct (4x sub_08069988(i) calls packing 2-bit field into *(u8*)result via (*p&~3)|(val&3)); first 48 bytes match exactly (prologue + first call), diverges starting 2nd sub_08069988 call - retail keeps mask constant 3 in r10 persistently reused across all 4 iterations, my C recomputes per-call causing different register allocation

## Current state

size_mismatch 62/154 (40.3%), compiled 152B vs retail 154B; logic correct (4x sub_08069988(i) calls packing 2-bit field into *(u8*)result via (*p&~3)|(val&3)); first 48 bytes match exactly (prologue + first call), diverges starting 2nd sub_08069988 call - retail keeps mask constant 3 in r10 persistently reused across all 4 iterations, my C recomputes per-call causing different register allocation

## Next

try extracting shared mask into persistent local var assigned once before all 4 blocks (e.g. u8 mask=3; u8 inv=~3;) or write as loop over array of 4 args to force agbcc register reuse pattern
