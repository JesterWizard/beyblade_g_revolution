# sub_0804B40C — WIP

| | |
|--|--|
| ROM | `0x0804B40C` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0804B40C.c` |
| Last `match_function.py` | 51/168 then 49/168
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: direct family seed 51/168 (176B), then fixed-offset r4 variant 49/168 (176B). Call sequence, constants, and signed-index behavior are mapped; register pinning worsened the prologue and retail's offset/first-coordinate lifetime remains unresolved.

## Current state

Two semantic attempts: direct family seed 51/168 (176B), then fixed-offset r4 variant 49/168 (176B). Call sequence, constants, and signed-index behavior are mapped; register pinning worsened the prologue and retail's offset/first-coordinate lifetime remains unresolved.

## Next

Use the UI-renderer family permuter: ordinary input in saved r4, image in saved r7, first coordinate in r6; avoid fixed asm pins that reorder the prologue, and force direct sub_080615EC expressions to remove coordinate narrowing.
