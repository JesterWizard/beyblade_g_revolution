# sub_08041858 — WIP

| | |
|--|--|
| ROM | `0x08041858` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08041858.c` |
| Last `match_function.py` | 47/52
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: 90% DIFF size mismatch (48 vs 52); agbcc folds gUnk_03000504 as gUnk_03000534-0x30 (subs r0,#0x30) instead of separate pc-relative ldr like retail; retail interleaves load-then-store for p1 before loading p2
- 2026-09-20 — tried a volatile p2 pointer, an `r0`-pinned p2 pointer, and
  a short decomp-permuter run; all retained the 48-byte output. No
  matched-file changes.

## Current state

90% DIFF size mismatch (48 vs 52); agbcc folds gUnk_03000504 as gUnk_03000534-0x30 (subs r0,#0x30) instead of separate pc-relative ldr like retail; retail interleaves load-then-store for p1 before loading p2. Volatile and `r0`-pinned p2 variants plus a short permuter run did not break the fold.

## Next

try forcing p2 load via a function call boundary or different intermediate type (u8* then cast) to break constant folding; or inline asm for just the final two stores
