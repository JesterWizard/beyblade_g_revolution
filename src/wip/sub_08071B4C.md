# sub_08071B4C — WIP

| | |
|--|--|
| ROM | `0x08071B4C` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08071B4C.c` |
| Last `match_function.py` | 73/84 bytes |
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: matched only with GCC asm labels; stripped DIFF
- 2026-09-21 — bounded permuter runs (300 s then 900 s, `--no-integrate`): no score 0
  (score 335 → 5). Remaining gap: the second RAM address must not fold into the first (`0x030040DC + 0x30`), and the four `str` stores want r4 while the `strh`s use r1.

## Current state

not matched — parked. Best permuter score 5 (from 335); `src/matched/` holds readable Thumb.

## Next

rewrite without register asm / empty asm(); permuter if same-size
