# sub_0802BC14 — WIP

| | |
|--|--|
| ROM | `0x0802BC14` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0802BC14.c` |
| Last `match_function.py` | 58/112
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: same-size DIFF (46/112, 41%); agbcc emits lsrs (u8 truncate) before bl sub_0802C62C where retail reuses sa register directly with plain adds; loop/null-check structure otherwise correct
- 2026-09-20 — used a signed-return function-pointer cast for the first
  `sub_0802C62C` call and pinned the record pointer to `r0`; best score
  improved to 58/112, but the remaining pool/register layout still differs.

## Current state

same-size DIFF (58/112, 51.8%); a signed-return function-pointer cast for
the first `sub_0802C62C` call and an `r0`-pinned record pointer improved the
score, but the remaining pool/register layout still differs.

## Next

leave readable Thumb; revisit with a verified `sub_0802C62C` signed-argument
prototype or a targeted pool/register permutation

- 2026-09-20 — seeded attempt (manual, no permuter): corrected a real
  type bug first — `sub_0802C62C` is declared `s32 sub_0802C62C(u8 a)`
  in include/unknown-functions.h (u8, not s8!), but this WIP's source
  was passing `sa` (s8) directly. Fixed to `sub_0802C62C((u8)sa)`
  (kept `sa` for `_0802BA7C(s8 a)` which does want s8). This did NOT
  change the byte score (still 46/112, 41.1%, same_size) but is a
  correctness fix worth keeping regardless of match status.
  Tried 3 phrasings of the u8/s8 split (separate `ua`/`sa` locals;
  `(u8)a` inline at call site before computing `sa`; `(u8)sa` cast) —
  only `(u8)sa` ties the original 46/112 baseline, the other two
  regress to 19.8%/22.4%.
  Root divergence (confirmed via objdump byte-diff) is unchanged from
  the original note: retail computes `sa` fully (lsls #24; asrs #24)
  THEN truncates back to u8 via a plain `lsrs r0,r5,#24` right before
  `bl sub_0802C62C`, i.e. the s8->u8 truncation happens AFTER the
  s8 sign-extension, immediately before the call, with NO separate
  asrs for the call arg itself. My best compile always either does
  the u8 truncation before the s8 sign-extension (wrong order) or
  duplicates the full lsls/asrs chain per-variable (extra bytes).
  Permuter candidate: seed with PERM_VAR on whether `(u8)a` or
  `(u8)sa` is used for the first call, combined with PERM_GENERAL on
  statement order (call before or after computing `sa`).
