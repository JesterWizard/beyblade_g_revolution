# sub_0802B994 — WIP

| | |
|--|--|
| ROM | `0x0802B994` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0802B994.c` |
| Last `match_function.py` | 8/58
| Sibling / types | |

## Role

Look up `gMainWorkPtr->unk1690->unk00` in the `0x0807709C` `{key, value}` ROM table (`bx lr` leaf).

## Process

- 2026-09-20 — parked. Status: dual-cursor ROM table is semantically right; agbcc still emits push {lr} (64B vs 58B bx lr leaf)
- 2026-09-20 — retried with register-pinned loc/key/table walk; still `00 b5` push {lr} (64 vs 58), 8/58. Mid-function literal pool between match and continue.

## Current state

dual-cursor ROM table is semantically right; agbcc still emits push {lr} (64B vs 58B bx lr leaf). Pool sits in the middle of the loop.

## Next

stay Thumb; same extra-push-on-leaf class as sub_0802D8C4 / sub_08061BDC
