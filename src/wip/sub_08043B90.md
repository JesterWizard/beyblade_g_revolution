# sub_08043B90 — WIP

| | |
|--|--|
| ROM | `0x08043B90` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08043B90.c` |
| Last `match_function.py` | 67/76
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: 67/76 same-size DIFF; sibling-shaped semantic C matches the loop and bytes, but agbcc keeps a two-byte mov/branch placement difference around the shared return path after the ROM literal pool

## Current state

67/76 same-size DIFF; sibling-shaped semantic C matches the loop and bytes, but agbcc keeps a two-byte mov/branch placement difference around the shared return path after the ROM literal pool

## Next

use an explicit label/goto layout that preserves r0 from the terminating unk00 load and aligns the return path with the literal-pool gap
