# sub_08071E84 — WIP

| | |
|--|--|
| ROM | `0x08071E84` |
| Retail | 96 B |
| `src/matched` | `src/matched/sub_08071E84.c` — **MATCHED** (old_agbcc) |
| Seed | `src/wip/sub_08071E84.c` |
| Last `match_function.py` | 96/96 |
| Sibling / types | `sub_08071EE4` (calls `sub_08071E04` instead of `sub_08071E44`) |

## Role

Finds the first free `Unk71E84` slot, calls `sub_08071E44`, stamps the next id from `gData_030000C8`, and returns it. Exhaustion calls `sub_08067B98` and returns -1.

## Process

- 2026-09-21 — **matched** with `old_agbcc`. Plain agbcc folds the entry test `(byte - 1) != -1` into `cmp r0,#0` (58/96). `for (i = *(u8 *)gData_030040C4 - 1; i != -1; i--)` keeps the materialised -1. `id` must be a local so `id++` reuses the value just stored into `unk18` instead of reloading the global.

## Current state

Matched and integrated; `make compare` OK.

## Next

Done.
