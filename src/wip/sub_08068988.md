# sub_08068988 — WIP

| | |
|--|--|
| ROM | `0x08068988` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08068988.c` |
| Last `match_function.py` | 37/126 then 124/126
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: initial ABI call shape 37/126 (128B), then passed through the original r2/r3/stack arguments to sub_08068A08 and reached 124/126, same size. The only remaining bytes are the two signed bound comparisons: retail emits bge while the candidate uses bcs because width/height were u32.

## Current state

Two semantic attempts: initial ABI call shape 37/126 (128B), then passed through the original r2/r3/stack arguments to sub_08068A08 and reached 124/126, same size. The only remaining bytes are the two signed bound comparisons: retail emits bge while the candidate uses bcs because width/height were u32.

## Next

Change width and height temporaries to s32 (state unk00/unk04 are signed), retaining the five-argument sub_08068A08 call with unused as its third argument. This should change both bcs instructions to the retail bge and yield an exact match.
