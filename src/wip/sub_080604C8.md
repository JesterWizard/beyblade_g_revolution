# sub_080604C8 — WIP

| Field | Value |
|-------|-------|
| ROM | `0x080604C8` |
| Size | 112 bytes |
| Seed | `src/wip/sub_080604C8.c` |
| Score | 92/112 (82.1%), same size, `old_agbcc` |
| Compiler | `old_agbcc` (plain agbcc 60/112) |

## Role

Packs six u8 pairs from the struct at `*gUnk_03000750` into BGR555-style u16
fields, then mirrors six u16 words into the IO block starting at `0x04000040`
(the first from the still-live `w` register, the rest re-read from the struct).

## Process

- 2026-09-22 — **rewritten from the disassembly** (previous seed guessed guards and
  a `w`-chasing pointer chain that are not in retail). Retail's actual shape:
  six `(u8 << 8) | u8` stores in the order `08, 0A, 0C, 0E, 14, 16`, then
  `*(vu16 *)0x04000040 = w;` followed by a 5-step cursor at `0x04000042` reading
  `unk0A/unk0C/unk0E/unk14/unk16` back out of the struct.
- Result: 92/112 same-size (was ~50/112 equivalent before).

## Current state

Only the tail register split differs:

```
retail:   ldr r2, =0x04000042   ldrh r0, [r1,#0x0A]   strh r0, [r2]   adds r2, #2 ...
compiled: ldr r0, =0x04000042   ldrh r2, [r1,#0x0A]   strh r2, [r0]   adds r0, #2 ...
```

i.e. retail coalesces the IO cursor with `w`'s dead register (r2) and loads values
into r0; agbcc keeps the value temp in `w`'s register and puts the cursor in r0.
Tried: `out` assigned before/after the `0x40` store, `out[i]` indexing,
`*++out`, explicit `out = out + 1`, `vu16 *out`, `u16*`/`u32` value temps,
`w = w - 0;`, split `*(u16 *)(gData_04000042 + n)` stores. All land at 92/112 with
the same swap.

## Next

- Permuter run in progress on this seed (same size, few-byte DIFF → its home turf).
- If that saturates: force the cursor pseudo to outlive `w` (e.g. an extra live use
  of `out` after the last store that the optimiser cannot drop) or find another
  caller of this packer and copy its register shape.
