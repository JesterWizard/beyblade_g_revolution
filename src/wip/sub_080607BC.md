# sub_080607BC — WIP

| | |
|--|--|
| ROM | `0x080607BC` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080607BC.c` |
| Last `match_function.py` | 26/128 then 36/128
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: direct typed state 26/128 (same size), then r2/r3/r4/r5 register anchors 36/128 (136B). State fields and blend behavior are mapped; remaining mismatches are repeated submode/delay loads, temp evaluation order for +0x02/+0x04, and the retail non-optimized 0xFFFF|old-word instruction shape.

## Current state

Two semantic attempts: direct typed state 26/128 (same size), then r2/r3/r4/r5 register anchors 36/128 (136B). State fields and blend behavior are mapped; remaining mismatches are repeated submode/delay loads, temp evaluation order for +0x02/+0x04, and the retail non-optimized 0xFFFF|old-word instruction shape.

## Next

Use local submode/delay/current/amount temporaries: submode=unk07, delay=unk08, current=unk02, amount=unk04; make mode a u32 in r4, state r2, location r3, and let the 0xFFFF OR reuse r5 naturally.
