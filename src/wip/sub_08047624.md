# sub_08047624 — WIP

| | |
|--|--|
| ROM | `0x08047624` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08047624.c` |
| Last `match_function.py` | 132/174 then 132/174
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two attempts produced the same 132/174 near-match (172B). Coordinate alignment semantics and signed fixed-point source fields are correct; the only observed size/layout delta is retail's extra common-label branch before the return.

## Current state

Two attempts produced the same 132/174 near-match (172B). Coordinate alignment semantics and signed fixed-point source fields are correct; the only observed size/layout delta is retail's extra common-label branch before the return.

## Next

Restore the semantic loop seed and use a branch-layout/permuter search to preserve the extra _080476AE common label branch; do not alter the alignment arithmetic.
