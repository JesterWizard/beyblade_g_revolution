# sub_080610A8 — WIP

| | |
|--|--|
| ROM | `0x080610A8` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080610A8.c` |
| Last `match_function.py` | 46/98 then 73/98
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: if-chain 46/98 (96B), then switch with unsigned half-shift 73/98 (same size). Guards, fields, call arguments, and comparison tree now match; remaining same-size diff is case body placement/order (retail lays out mode 0, mode 2, mode 1 before the shared sub_0806105C tail).

## Current state

Two semantic attempts: if-chain 46/98 (96B), then switch with unsigned half-shift 73/98 (same size). Guards, fields, call arguments, and comparison tree now match; remaining same-size diff is case body placement/order (retail lays out mode 0, mode 2, mode 1 before the shared sub_0806105C tail).

## Next

Keep the switch comparison and unsigned shift; reorder source case bodies to case 0, case 2, case 1 (or use gotos) to reproduce retail labels _080610E0/_080610EC/_080610F4 and shared tail.
