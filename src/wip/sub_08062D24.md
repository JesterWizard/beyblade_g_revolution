# sub_08062D24 — WIP

| | |
|--|--|
| ROM | `0x08062D24` |
| Retail | |
| `src/matched` | **semantic C** |
| Seed | `src/wip/sub_08062D24.c` |
| Last `match_function.py` | 42/42 — MATCH |
| Sibling / types | |

## Role

Unpack one BGR555 palette entry from VRAM (`0x05000000 + idx*2`) into three 5-bit bytes.

## Process

- 2026-09-20 — parked. Status: matched only with GCC asm labels; stripped DIFF
- 2026-09-21 — **matched**. permuter: re-materialize `shifted` through a pointer local before adding the palette base (the plain `shifted + base` form allocates r0 instead of r2).

## Current state

matched — semantic C in `src/matched/` (100%).

## Next

done.
