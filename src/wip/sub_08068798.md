# sub_08068798 — WIP

| | |
|--|--|
| ROM | `0x08068798` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08068798.c` |
| Last `match_function.py` | 40/110 then 70/110
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: initial queue model 40/110 (116B), then pinned offset/entry/callback registers and matched the body at 70/110 (112B). The remaining mismatch is the missing r7 save/restore in the generated prologue; retail uses f0b5 while agbcc emits 70b5, shifting the early branch and final size by 2 bytes.

## Current state

Two semantic attempts: initial queue model 40/110 (116B), then pinned offset/entry/callback registers and matched the body at 70/110 (112B). The remaining mismatch is the missing r7 save/restore in the generated prologue; retail uses f0b5 while agbcc emits 70b5, shifting the early branch and final size by 2 bytes.

## Next

Remove the explicit r7 register pin from count and make count a normal live local across _08073C48; preserve r6 state, r5 index, r4 entry, and r1 offset/r0 table base. This should let agbcc save r7 while retaining the 70/110 body shape.
