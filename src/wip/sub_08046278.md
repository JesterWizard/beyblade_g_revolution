# sub_08046278 — WIP

| | |
|--|--|
| ROM | `0x08046278` |
| Retail | 92B (`push {r4-r7,lr}` + `r8`/`r9`, no `bl`) |
| `src/matched` | readable Thumb (naked) |
| Seed | `src/wip/sub_08046278.c` |
| Last `match_function.py` | **DIFF** 11/92 (12.0%), size 56 vs 92 |
| Sibling | `sub_08046230` **MATCH** — same `MainWork.unk16B0[2]` / `struct Unk16B0` |

## Role

Unconditional init of both `gMainWorkPtr->unk16B0[]` slots to `{unk00=0, unk04=-1, unk08=-1}`. `sub_08046230` does the same three stores for **one** index, and only if `unk00 == 1`.

Retail loop (Thumb): `r3` starts at 1, `r2` byte-offset 0 then `+= 0xC`, three **independent** pool addends `0x16B0` / `0x16B4` / `0x16B8` off `*gMainWorkPtr` (`0x03000198` in `r8`), store 0 from `r9` then `-1` from `r4`.

## Process

- 2026-09-20 (prior session) — m2c failed. Hand C could not reproduce retail’s extra `r8`/`r9` spill on a leaf with no calls. Draft **reverted** from `src/matched/` with no seed saved (the gap this park fixes).
- 2026-09-20 (park) — reconstructed logic from the sibling MATCH + the naked Thumb in `src/matched/sub_08046278.c`. Seed is the clear `for (i = 0; i < 2; i++)` form using `unk16B0[i].unkNN` (same member style that landed `sub_08046230`). `match_function.py`: **11/92**, compiled 56B vs retail 92B (`size_mismatch`). The shortfall is the missing `r8`/`r9` live range plus independent pool addends — agbcc emits `push {r4,lr}` (56B), retail `push {r4-r7,lr}` + `r8,r9` (92B).

## Current state

- Types are already in `unknown-types.h` (`Unk16B0`, `MainWork.unk16B0[2]`).
- Logic of the parked C is believed correct; bytes **do not** match (11/92, 56B vs 92B). Matching needs:
  1. Independent per-field addressing (do **not** cache `unk16B0[i]` — `sub_08046230` lesson).
  2. Extra live `r8`/`r9` (pool `gMainWorkPtr` + zero constant) so the prologue matches `push {r4-r7,lr}` + `r8,r9`.
  3. Countdown `i = 1; do … while (i >= 0)` and byte `off += 0xC` rather than `for (i = 0; i < 2)`.

## Next

1. Rewrite the seed toward retail evaluation order (do not re-disassemble from scratch): byte `off` loop, three separate `gMainWorkPtr->unk16B0` field stores, `tmp[]` / `register asm("r8")` for `0x03000198` if the pool load vanishes.
2. Re-run `match_function.py` and append the new N/M here.
3. If still same-size DIFF after that shape → permuter from this seed.
