# sub_080312B0 — WIP

| | |
|--|--|
| ROM | `0x080312B0` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_080312B0.c` |
| Last `match_function.py` | 36/40
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: 90% DIFF; retail keeps b->unk14 load in r5 (separate reg from shift result r0), push{r4,r5,lr}; compiled agbcc always reuses r0/collapses r5, push{r4,lr}

## Current state

90% DIFF; retail keeps b->unk14 load in r5 (separate reg from shift result r0), push{r4,r5,lr}; compiled agbcc always reuses r0/collapses r5, push{r4,lr}

## Next

try forcing volatile u16 val, or restructure as ternary/early-return shape; possibly needs the shift expressed via macro that references b->unk14 twice to force reload
