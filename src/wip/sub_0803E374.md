# sub_0803E374 — WIP

| | |
|--|--|
| ROM | `0x0803E374` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0803E374.c` |
| Last `match_function.py` | 40/76
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: 40/76 same-size DIFF; cloned signed table lookup matches size and most arithmetic, but agbcc still schedules the 0x0807BDB8 literal after the 0x1E index setup despite r0/r1 pins

## Current state

40/76 same-size DIFF; cloned signed table lookup matches size and most arithmetic, but agbcc still schedules the 0x0807BDB8 literal after the 0x1E index setup despite r0/r1 pins

## Next

try a dependency-preserving table-base load before the first signed index (or reuse the successful 3E328 pattern with a forced r0 table local); the remaining table accesses are offset +1
