# sub_0803E374 — WIP

| | |
|--|--|
| ROM | `0x0803E374` |
| Retail | clone of `sub_0803E328` with byte offset +1 (`ldrb r1,[r1,#0x01]`) |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0803E374.c` (`/* match-compiler: old_agbcc */`) |
| Last `match_function.py` | 41/76 same-size (old_agbcc) |
| Sibling / types | `sub_0803E328` (+0), `sub_0803E3C0` (+2) |

## Role

Same byte-offset table sum as `sub_0803E328`, offset +1.

## Process

- 2026-09-21 — retargeted to the `old_agbcc` "split" shape that scores 47/76 on
  `sub_0803E328`. Here it gives 41/76 same-size (previous parked seed: 40/76).

## Current state

41/76 same-size, `old_agbcc`.

## Next

`sub_0803E328` is the family leader — solve that one and copy the shape here.
