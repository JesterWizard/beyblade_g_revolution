# sub_0803E374 — WIP

| | |
|--|--|
| ROM | `0x0803E374` |
| Retail | 76 B clone of `sub_0803E328` with `ldrb [rN, #1]` |
| `src/matched` | `src/matched/sub_0803E374.c` — **MATCHED** (old_agbcc) |
| Seed | `src/wip/sub_0803E374.c` |
| Last `match_function.py` | 76/76 (`old_agbcc`) |
| Sibling / types | `sub_0803E328` (+0), `sub_0803E3C0` (+2); `struct Unk3E374Row` |

## Role

Sums byte 1 of three 4-byte table rows (indexed by `unk1E`, signed `unk20`, `unk1D`) and returns `_080741EC(sum, 3) - 1`.

## Process

- 2026-09-21 — **matched**. `table[scaled + 1]` makes old_agbcc emit `adds r0,#1` before the base add (80 B). A `struct` member (`->unk01`) puts the `+1` back on `ldrb [rN,#1]`.
- The remaining miss was pool-load order on the 2nd and 3rd tables: retail does `ldr r3,=table` *before* `movs r1,#imm` / `ldsb`. A discarded `v2 = table[index];` immediately before the real member read hoists that load. Both discards are required.

## Current state

Matched and integrated; `make compare` OK.

## Next

Done. Same shape with `unk02` is `sub_0803E3C0`.
