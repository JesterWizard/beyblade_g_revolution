# sub_0806184C — WIP

| | |
|--|--|
| ROM | `0x0806184C` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0806184C.c` |
| Last `match_function.py` | 47/92 then 80/92
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two attempts: initial semantic seed 47/92 (96B), then signed-width and shifted-stride corrections 80/92 (same-size). Bounds, VRAM layout, and copy call now match; remaining diff is only bank-byte/address register order (retail loads bank into r0 then shifts into r1, candidate loads into r1).

## Current state

Two attempts: initial semantic seed 47/92 (96B), then signed-width and shifted-stride corrections 80/92 (same-size). Bounds, VRAM layout, and copy call now match; remaining diff is only bank-byte/address register order (retail loads bank into r0 then shifts into r1, candidate loads into r1).

## Next

Pin the bank temporary to r0 (or otherwise force `ldrb r0,[state+0x5D]`), retain u16 width and `((u32)width << 5) * y`; this should reproduce the final address instructions.
