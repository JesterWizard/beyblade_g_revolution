# sub_08032DC4 — WIP

| | |
|--|--|
| ROM | `0x08032DC4` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08032DC4.c` |
| Last `match_function.py` | 418/660
| Sibling / types | Unk346C0 battle frame loop |

## Role

Battle frame loop: asset refresh, per-frame motion/update, and three conditional mode handlers.

## Process

- 2026-09-20 — parked. Status: register-pinned candidate reached 413/660 bytes (656-byte output).
- 2026-09-20 — assign `state_a`, `state_b`, then `frame` so retail `adds r6, r1; mov r9, r2; mov r8, r0` matches. Score 418/660 (still 656B). Mask-first keys rewrite shrank to 636B; reverted.

## Current state

418/660 (656 vs 660). Arg copies match. Remaining 4-byte shortage shifts pool offsets; keys-held test still loads the halfword before `movs #8`.

## Next

Find the missing 4-byte insn without shrinking the rest; permuter on mask-first `movs r0,#8` before `ldrh` of `gBtlKeysHeld`.
