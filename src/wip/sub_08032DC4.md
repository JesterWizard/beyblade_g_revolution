# sub_08032DC4 — WIP

| | |
|--|--|
| ROM | `0x08032DC4` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08032DC4.c` |
| Last `match_function.py` | 413/660
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Battle frame-loop semantics reconstructed across asset refresh, per-frame motion/update calls, and three conditional mode handlers; register-pinned candidate reached 413/660 bytes (656-byte output).

## Current state

Battle frame-loop semantics reconstructed across asset refresh, per-frame motion/update calls, and three conditional mode handlers; register-pinned candidate reached 413/660 bytes (656-byte output).

## Next

Preserve the r6/r9/r8 argument pinning and tune only the frame initialization/spill plus stack-struct field types; the remaining diff is mostly register/literal scheduling.
