# sub_08042B50 — WIP

| | |
|--|--|
| ROM | `0x08042B50` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08042B50.c` |
| Last `match_function.py` | 35/40
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: 35/40 same-size DIFF; clone logic and register pins reproduce the exact size, but agbcc still places the table-base literal after the index shift instead of retail's r0-before-r1 sequence

## Current state

35/40 same-size DIFF; clone logic and register pins reproduce the exact size, but agbcc still places the table-base literal after the index shift instead of retail's r0-before-r1 sequence

## Next

reuse the 42B28 selective base/index shape with a dependency that prevents scheduling the index shift before the ROM literal load
