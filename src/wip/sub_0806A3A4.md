# sub_0806A3A4 — WIP

| | |
|--|--|
| ROM | `0x0806A3A4` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_0806A3A4.c` |
| Last `match_function.py` | 119/144 then 123/144
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts after isolating the stale one-argument header declaration: direct clone 119/144 (140B), then ordered current_value/r2 pool load reached 123/144 (140B). State initialization and allocator call now match; remaining mismatch is the second counter zero store, where retail loads a separate 0x03000B38 literal but agbcc reuses the 0x03000B3C address with add/sub 4.

## Current state

Two semantic attempts after isolating the stale one-argument header declaration: direct clone 119/144 (140B), then ordered current_value/r2 pool load reached 123/144 (140B). State initialization and allocator call now match; remaining mismatch is the second counter zero store, where retail loads a separate 0x03000B38 literal but agbcc reuses the 0x03000B3C address with add/sub 4.

## Next

Force distinct typed pointer literals for 0x03000B3C and 0x03000B38 (or use a small helper boundary that prevents adjacent-address folding), retaining current_value in r1 and pool address in r2. Then compare the post-node sentinel pointer arithmetic.
