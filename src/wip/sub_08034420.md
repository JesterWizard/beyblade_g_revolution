# sub_08034420 — WIP

| | |
|--|--|
| ROM | `0x08034420` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08034420.c` |
| Last `match_function.py` | 76/328
| Sibling / types | Unk346C0 / Unk346C0Inner / Unk310F0Inner2.unk24 |

## Role

Initialize an Unk346C0 battle object from two Unk346C0Inner states and a mode byte.

## Process

- 2026-09-20 — parked. Status: Battle object initialization semantics reconstructed using the existing Unk346C0 layout; candidate is near-size (204/328 bytes, 324-byte output) after correcting the fixed-point defaults.
- 2026-09-20 — u32 mode, r6/r8 pins, ldsb then ldrb+extend for unk24. Prologue now matches (`push {r4-r7,lr}; mov r7,r8; push {r7}`). Best 76/328 same-size.

## Current state

76/328 same-size. Remaining: after `adds r5, r5, r6; mov r8, r5` retail does `str r1, [r5]` (zero unk2D0 through r5). Candidate reloads `mov r0, r8; str r1, [r0]`.

## Next

Keep r5 live for the unk2D0 zero-store after `mov r8, r5`; preserve u32 mode and the signed/unsigned unk24 pair.
