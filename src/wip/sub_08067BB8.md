# sub_08067BB8 — WIP

| | |
|--|--|
| ROM | `0x08067BB8` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/wip/sub_08067BB8.c` |
| Last `match_function.py` | 113/304 then 44/304 |
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic constructor attempts: structured field initialization reached 113/304 (284B), then register-pinned zero/all-ones constants regressed to 44/304 (284B). Retail layout and initialization order are mapped; remaining ABI/register issue is preserving r4/r5/r6 stack arguments before reusing r4/r5 and forcing the literal-mask OR sequence.

## Current state

Two semantic constructor attempts: structured field initialization reached 113/304 (284B), then register-pinned zero/all-ones constants regressed to 44/304 (284B). Retail layout and initialization order are mapped; remaining ABI/register issue is preserving r4/r5/r6 stack arguments before reusing r4/r5 and forcing the literal-mask OR sequence.

## Next

Retry with register locals declared but initialized only after the three shifted stack arguments are stored; pin zero to r4, byte_zero to r5, and allones to r2. Compare the exact prologue and the `unk60 |= allones`, `unk1A |= allones`, and `unk2E |= allones` loads before integrating.
