# sub_08067F98 — WIP

| | |
|--|--|
| ROM | `0x08067F98` |
| Retail | |
| `src/matched` | **matching C, register-shaped** (`match-compiler: old_agbcc`) |
| Seed | `src/decompiled/sub_08067F98.c` |
| Last `match_function.py` | 48/48 — MATCH (old_agbcc) |
| Sibling / types | |

## Role

Variable-stride record search: walk the `Unk68014` table for a 16-bit key, following each record's `unk02` stride.

## Process

- 2026-09-20 — parked. Status: matched only with GCC asm labels; stripped DIFF
- 2026-09-21 — semantic rewrite attempt: natural `Unk68014Rec` walk (for/while/do-while, u16 vs u32 key) is a same-size DIFF (best 6/48 bytes); a 600 s bounded permuter run from the semantic seed did not reach score 0 (best same-size 6/48).
- 2026-09-21 — **matched**. The parked seed is byte-exact when compiled with `old_agbcc`; `agbcc` picks a different register for the load it coalesces into the consuming instruction. Landed in `src/matched/` with `/* match-compiler: old_agbcc */`.

## Current state

matched — the register-shaped C in `src/matched/` is byte-exact under `old_agbcc`; a purely semantic rewrite of the record walk is still an open same-size DIFF.

## Next

done.
