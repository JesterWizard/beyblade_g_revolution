# sub_08033574 — WIP

| | |
|--|--|
| ROM | `0x08033574` |
| Retail | |
| `src/matched` | **semantic C** (`match-compiler: old_agbcc`) |
| Seed | `src/decompiled/sub_08033574.c` |
| Last `match_function.py` | 80/80 — MATCH (old_agbcc) |
| Sibling / types | |

## Role

BattleWork end-of-turn flag teardown: when `unk2088 == 1`, pass `unk1FE6 >> 1` to `sub_08038638` after `sub_08068808`, then clear `unk2088`/`unk2089`.

## Process

- 2026-09-20 — parked. Status: 20/80 after embedded-state modeling; semantic field map is plausible, but agbcc uses r4/r5 and omits retail r6 while the retail prologue keeps the global pointer in r5, base in r1, and 0x2088 in r6
- 2026-09-21 — **matched**. The parked seed is byte-exact when compiled with `old_agbcc`; `agbcc` picks a different register for the load it coalesces into the consuming instruction. Landed in `src/matched/` with `/* match-compiler: old_agbcc */`.

## Current state

matched — semantic C in `src/matched/` (100%, `old_agbcc`).

## Next

done.
