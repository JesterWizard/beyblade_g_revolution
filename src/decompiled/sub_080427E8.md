# sub_080427E8 — WIP

| | |
|--|--|
| ROM | `0x080427E8` |
| Retail | |
| `src/matched` | **semantic C** (`match-compiler: old_agbcc`) |
| Seed | `src/decompiled/sub_080427E8.c` |
| Last `match_function.py` | 220/220 — MATCH (old_agbcc) |
| Sibling / types | |

## Role

Ring-mode callback dispatch (`0x20`/`0x40`/`0x80`/`0x100`) on `gUnk_03000538->unk02`, setting the `MainWork.unk0479` mode bits.

## Process

- 2026-09-20 — parked. Status: Two attempts: switch-based semantic seed 119/220 (same-size), then r2-pinned MainWork/direct ring mode 98/220 (216B). Behavior is mapped across ring modes 0x20/0x40/0x80/0x100; remaining differences are register/literal scheduling and duplicated callback/flag block layout.
- 2026-09-21 — **matched**. The parked seed is byte-exact when compiled with `old_agbcc`; `agbcc` picks a different register for the load it coalesces into the consuming instruction. Landed in `src/matched/` with `/* match-compiler: old_agbcc */`.

## Current state

matched — semantic C in `src/matched/` (100%, `old_agbcc`).

## Next

done.
