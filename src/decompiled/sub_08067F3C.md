# sub_08067F3C — WIP

| | |
|--|--|
| ROM | `0x08067F3C` |
| Retail | 92 B. Returns 0 when `v >= inner->unk08`; otherwise `unk04 * unk02`, plus a run of halfwords at `sub_08068884(inner) + unk00` when that pointer is non-NULL, bit 2 of `unk98` is clear, and `unk02 != 0` |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08067F3C.c` |
| Last `match_function.py` | 58/92, compiled 96 |
| Sibling / types | `struct Unk67F3C`, `Unk68014`, `Unk68014Rec`; calls `sub_08068014`, `sub_08068884` |

## Role

Sums a rectangle of halfwords for one animation record, or returns 0 when `v` is past `unk08`.

## Process

- 2026-09-21 — best seed is 58/92 (4 bytes over). Retail keeps the product in r2 (`muls r2, r3`, unk02 in r3) and counts the loop with `subs r1, #1`. This seed puts the product in r3 and decrements the `u16` counter with `lsls`/`lsrs`.
- An `s32` counter, a separate `count = n`, and a mask-first `unk98 & 4` all scored worse (down to 25/92).

## Current state

58/92, compiled 96 vs retail 92.

## Next

Do not hand-chase the multiply's destination register. Revisit only from a same-size seed.
