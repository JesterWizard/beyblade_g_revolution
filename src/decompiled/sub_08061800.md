# sub_08061800 — WIP

| | |
|--|--|
| ROM | `0x08061800` |
| Retail | 76 B: bounds-checks `arg0` against `(gUnk_03000798->unk9A >> 3) - 1`, then calls `_08073C4C` with a BG2 tile address built from `unk5D << 14` and `arg0 * stride + 0x06000000` |
| `src/matched` | `src/matched/sub_08061800.c` — **MATCHED** (old_agbcc) |
| Seed | `src/decompiled/sub_08061800.c` |
| Last `match_function.py` | 76/76 (`old_agbcc`) |
| Sibling / types | `_08073C4C` is a `bx r3` trampoline (precedent: `sub_08035884.c`); 4th arg is a handler pointer |

## Role

Publishes one row of the current layout into BG2 tile memory.

## Process

- 2026-09-21 — **matched** with `old_agbcc` once `off = arg0 * stride + 0x06000000;`
  exists as its own local. The accumulator order is the whole delta: retail forms
  `arg0*stride + 0x06000000` first and adds `lo` last, while left-associating the sum
  in one expression (`lo + arg0 * stride + 0x06000000`) makes agbcc accumulate
  `lo + arg0*stride` first.

## Current state

Matched and integrated; `make compare` OK.

## Next

Done.
