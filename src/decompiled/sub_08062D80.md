# sub_08062D80 — WIP

| | |
|--|--|
| ROM | `0x08062D80` |
| Retail | 264 B |
| `src/matched` | `src/matched/sub_08062D80.c` — **MATCHED** |
| Seed | `src/decompiled/sub_08062D80.c` |
| Last `match_function.py` | 264/264 |
| Sibling / types | `sub_08062E88` (`sub_08062CC8` / `sub_08062CF4`) |

## Role

For each palette index from `a` to `b`, adds or subtracts `d` from the RGB555 triplet and clamps (31 or 0).

## Process

- 2026-09-21 — **matched**. `if/else` saved `a` and inverted the `c` test (284 B). Gotos reproduce retail's `beq`/`beq`/`b`. The three stores must happen before the clamps so the first sum stays in r1. `(s8)v0` shifts via r0 (`lsls r0, r1`); `v0 <<= 24; v0 >>= 24` shifts r1 in place.

## Current state

Matched and integrated; `make compare` OK.

## Next

Done.
