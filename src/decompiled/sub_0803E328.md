# sub_0803E328 — WIP

| | |
|--|--|
| ROM | `0x0803E328` |
| Retail | `ldr r0,=0x0807BDB8` before `movs r1,#0x1E`; table1 base in r0, table2 in r3, `a` in r2 |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_0803E328.c` (`/* match-compiler: old_agbcc */`) |
| Last `match_function.py` | 47/76 same-size (old_agbcc) |
| Sibling / types | `Unk3E328` (`unk1D`/`unk1E` s8, `unk20` u8); clone family with `sub_0803E374` (+1) and `sub_0803E3C0` (+2) |

## Role

Byte-offset table sum: `t2[(s8)a->unk20*4 + OFF] + t1[a->unk1E*4 + OFF] + t3[a->unk1D*4 + OFF]`, then `_080741EC(v, 3) - 1`.

## Process

- 2026-09-21 — `old_agbcc` + the "split" shape (index into a local, load tables by variable)
  lifts this from 30/76 (agbcc, parked) to **47/76**. Plain `agbcc` on the same source: 30/76.
- `a` lands in r2 on both compilers; retail keeps `t1` base in r0 and reuses r3 for `t3`,
  agbcc reloads the `t1` base late (r3) and reuses r2 for `t3`.
- Remaining diff is a pure literal-pool/schedule ordering: retail loads the `0x0807BDB8`
  base 3 instructions earlier than agbcc does.
- Permuter 360 s x 2 jobs from this seed: base 475, no score 0.

## Current state

47/76 same-size, `old_agbcc`. Same shape matches for the +1/+2 siblings at 41/76.

## Next

Keep the split shape and try to make the first table base live from the top of the
function (e.g. compute the `unk1E` index against a hoisted base pointer), or permute
longer from this seed. `sub_0803E374` / `sub_0803E3C0` share the answer.
