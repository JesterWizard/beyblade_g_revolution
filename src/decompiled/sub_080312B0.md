# sub_080312B0 — WIP

| | |
|--|--|
| ROM | `0x080312B0` |
| Retail | |
| `src/matched` | **semantic C** (`match-compiler: old_agbcc`) |
| Seed | `src/decompiled/sub_080312B0.c` |
| Last `match_function.py` | 40/40 — MATCH (old_agbcc) |
| Sibling / types | |

## Role

Initialize an `Unk312EC` from `Unk705DC` metadata (copy `c`/`d`, snapshot `b->unk14 >> 0xC`, set the live flag).

## Process

- 2026-09-20 — parked. Status: matched only with GCC asm labels; stripped DIFF
- 2026-09-21 — **matched**. The parked seed is byte-exact when compiled with `old_agbcc`; `agbcc` picks a different register for the load it coalesces into the consuming instruction. Landed in `src/matched/` with `/* match-compiler: old_agbcc */`.

## Current state

matched — semantic C in `src/matched/` (100%, `old_agbcc`).

## Next

done.
