# sub_0807309C — WIP

| | |
|--|--|
| ROM | `0x0807309C` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_0807309C.c` |
| Last `match_function.py` | 11.7
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-22 — parked. Status: agbcc hoists the count load + folds i=0 into a separate register; retail keeps table in r5, i=0 in r6, count value in r2. Structure matches (for-loop inversion into do-while) but allocation differs

## Current state

agbcc hoists the count load + folds i=0 into a separate register; retail keeps table in r5, i=0 in r6, count value in r2. Structure matches (for-loop inversion into do-while) but allocation differs

## Next

read notes; match_function.py this seed
