# sub_08057274 — WIP

| | |
|--|--|
| ROM | `0x08057274` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08057274.c` |
| Last `match_function.py` | 80/208 then 81/208
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: direct MainWork switch 80/208 (204B), then r4 main-location pin 81/208 (204B). Case semantics and mode keys match; retail retains location in r4 and main pointer in r2, while agbcc reuses r4 for main, causing the remaining register/size layout delta.

## Current state

Two semantic attempts: direct MainWork switch 80/208 (204B), then r4 main-location pin 81/208 (204B). Case semantics and mode keys match; retail retains location in r4 and main pointer in r2, while agbcc reuses r4 for main, causing the remaining register/size layout delta.

## Next

Use explicit ordinary/register layout search for r4=0x03000198 location and r2=loaded MainWork pointer (pin main to r2 if needed), preserving the switch tree and shared 0x80/0x100 tail.
