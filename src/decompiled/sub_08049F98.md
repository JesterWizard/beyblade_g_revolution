# sub_08049F98 — WIP

| | |
|--|--|
| ROM | `0x08049F98` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08049F98.c` |
| Last `match_function.py` | unscored
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: same-size DIFF 36/144 (25%); logic confirmed correct (2x sub_080617C4 header calls, sub_080615EC coord set via idx<<4 chain, sub_08061784 return, sub_0806171C draw); agbcc keeps out-param in r8 across 3 calls in retail (push r8 at entry) but my C only uses out at final call so agbcc doesn't preallocate r8 early — retail's r8 usage implies out is referenced earlier in source or a dummy read keeps it live

## Current state

same-size DIFF 36/144 (25%); logic confirmed correct (2x sub_080617C4 header calls, sub_080615EC coord set via idx<<4 chain, sub_08061784 return, sub_0806171C draw); agbcc keeps out-param in r8 across 3 calls in retail (push r8 at entry) but my C only uses out at final call so agbcc doesn't preallocate r8 early — retail's r8 usage implies out is referenced earlier in source or a dummy read keeps it live

## Next

try touching 'out' earlier (e.g. volatile read) or reorder params so out is 2nd arg; also try casting idx shift result through s16 (retail uses lsrs #17 = arithmetic sign truncate to s15 for the return-value cast, suggesting val type may be s16 not u16 -- already tried, needs recheck combined with r8 fix). Twin clones sub_0804E17C/sub_08051578 share same structure - crack this one first, then just re-run match_function.py with same seed

- 2026-09-20 — seeded attempt (manual, no permuter): CRACKED the r8-liveness
  root cause. Introducing a 4th separately-named local (`p0` holding the
  original `idx<<4 + 8` pointer, kept alongside `p` which advances by
  0x10 for the middle call) forces agbcc to need 4 concurrent live
  registers across the `bl` calls (r4=p, r5=const 0x083A734C, r6=p0,
  r8=out) — this DOES reproduce retail's exact `mov r6,r8; push{r6}` /
  `mov r8,r2` prologue and the matching `pop{r3}; mov r8,r3` epilogue.
  Confirmed via byte diff: prologue/epilogue is now byte-identical to
  retail.
  Score regressed 36/144→34/144 (152B compiled vs 144B retail) because
  introducing `p0` as a separate variable makes agbcc insert 3x
  redundant `lsls rX,rX,#16 / lsrs rX,rX,#16` (u16-truncate) sequences
  when passing p/p0 to `sub_080615EC`, which retail does NOT do (it
  passes the raw r6/r4 register untruncated). Also confirmed the
  known `lsrs r1,r1,#17` (not #16) mismatch on `val`'s post-call mask
  is still present and unresolved — try val as s16 return cast
  combined with NOT re-declaring p0 (find another way to keep 4 live
  values without triggering the extra truncation, e.g. pass p/p0 as
  `u32` locals instead of `u8 *` to dodge implicit pointer-truncation
  logic, or use `(u16)` only at the sub_0806171C call sites instead of
  at sub_080615EC call sites).
  Seed used: `p0 = (u8*)(idx<<4); p0 += 8; p = p0;` ... `p += 0x10` for
  middle call ... `sub_080615EC(0,(u16)(u32)p0)` for final call.
  Apply the r8-forcing pattern (separate 4th live local) to
  sub_0804E17C/sub_08051578/sub_08053218 once the truncation issue is
  solved here — same structural fix should transfer.
