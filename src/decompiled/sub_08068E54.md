# sub_08068E54 — WIP

| | |
|--|--|
| ROM | `0x08068E54` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08068E54.c` |
| Last `match_function.py` | 17/108 then 70/108
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: direct motion model 17/108 (112B), then pinned state/x/y/scale registers reached 70/108 with exact size. Remaining mismatch is only the flag check register order: retail forms &state->unk64 in r1, loads the halfword into r1, and masks with r0; the candidate uses r0 for the address.

## Current state

Two semantic attempts: direct motion model 17/108 (112B), then pinned state/x/y/scale registers reached 70/108 with exact size. Remaining mismatch is only the flag check register order: retail forms &state->unk64 in r1, loads the halfword into r1, and masks with r0; the candidate uses r0 for the address.

## Next

Use a register u16 pointer flag_ptr pinned to r1 and assign it with &state->unk64 before the bit test. Keep state r4, scale/delta_y r3, x r1, y r2, and delta_x r5; this should reproduce the remaining flag-check bytes.
