# sub_08040F4C — WIP

| | |
|--|--|
| ROM | `0x08040F4C` |
| Retail | `push {r4,r5,lr}`; `done` in r5 (`movs r5,#0`) |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08040F4C.c` |
| Last `match_function.py` | 322/336 same-size (`agbcc`; prologue matches) |
| Sibling / types | `Unk40F4C` large stack state |

## Role

Menu/state loop: CpuSet-clear a stack object, then blend/mode dispatch until `done`.

## Process

- Logic matches. Extra callee-saved: retail `push {r4,r5,lr}` vs `push {r4,lr}` and `done` in r4 not r5.
- Dummy `pad` DCE'd (`done = pad` folded to 0).
- 2026-09-25 — delayed `pad=0` in blend1 + `done != pad` is 322/336. Independent `r3=0` still DCE. `done != 0` drops r5 (319/336). Permuter 60s best 225, still 322/336.

## Current state

322/336 same-size. Prologue matches (`push {r4,r5,lr}`; `done` in r5). Remaining: blend1 missing `movs r3,#0` before `strh`; check_done `cmp r5,r4` vs `cmp r5,#0` when `done != pad`.

## Next

Retail-order baseline stays **319/336** (`agbcc`). The permuter (300s ×2, base scores
700 and 255) and prologue variant sweep did not reach 100%.

### 2026-09-24 — prologue fork (partial)

Two incompatible prologue shapes:

| Shape | Score | `push` | `movs r5,#0` @+0x6 | CpuSet before `6639C` |
|-------|-------|--------|---------------------|------------------------|
| Retail order (`done=0` → CpuSet → `6639C`) | **319/336** | `{r4,lr}` | no (`movs r4,#0`) | yes |
| `done=0` → `6639C()` → CpuSet | **225/336** | `{r4,r5,lr}` | **yes** | no |

The `225/336` seed matches retail through byte +0x7 (`30 b5 … 00 25`) then diverges
because `bl sub_0806639C` occupies +0x8 where retail has the `gData_080BB8BC` pool load.
Inlining `6639C` before CpuSet also gets `30b5`/`0025` but grows to **356 B** (+20).

Tried without success: `frameSize` live-range split, fake `setupArg` placeholders,
`sibling sub_080632F8` inline-`6639C` shape, `unk2D4`-only exit (no `done` local),
semantic struct-member rewrite, blend1 zero locals, `static setupArg`, `-fprologue-bugfix`,
`old_agbcc`, raw literals vs `gData_*`, 20+ prologue orderings via `test_variants.py`.

**Blocker:** need `done` in **r5** (so `blend1` can use `movs r4,#0`) *and* retail
init order (`movs r5,#0` → CpuSet → `bl 6639C`). Expressing both in C seems to require
the allocator to reserve r5 from the early `6639C` call while emitting that call only
after CpuSet — not reachable with tested C/permuter shapes.

Park alternate seed `done_then_early6639` in notes: copy prologue from
`done=0; setupArg=sub_0806639C();` before CpuSet if a future permuter macro pass can
swap statement order without losing r5 allocation.

## 2026-09-24 — extern / gData retry

- Swapped raw `0x080BB8BC` / `0x080BB888` for `gData_080BB8BC` / `gData_080BB888`
  (same pattern as matched `sub_08060758`, `sub_08061E8C`, `TextMeasureWidth`'s
  `gData_080BB748`). Seed uses `/* match-compiler: old_agbcc */`.
  Retail-order: **315/336** (`old_agbcc`) vs **319/336** (`agbcc`).
- Hoisting `clearVal = *clearSym` before CpuSet (sub_08038580-style) drops to **313/336**
  — wrong pool scheduling for this function.
- Early `setupArg = sub_0806639C()` before `done = 0` + gData: only shape that gets
  `push {r4,r5,lr}` / `done` in r5, but init order wrong (**223/336**). Permuter 120s
  on that seed: base score 915, no improvement.
- TextMeasureWidth's extern win was a **pool destination register** fix (`gData` subscript +
  `const u8 *` + `old_agbcc`). Here the blocker is **callee-saved count** (`done` must
  occupy r5 while r4 stays free for blend1 zeros), which symbol names alone do not fix.

## 2026-09-24 — retry

- Confirmed 319/336 on `src/decompiled/sub_08040F4C.c`; permuter 300s, base score 255, no
  improvement.
- Tried: `blendActive`/`keep`/`clearSrc`/`argHold`/`frameSize` locals, `setupArg` after
  CpuSet, `volatile keep`, pool pointer in r4 — all 319/336 or worse.
- Breakthrough (partial): `earlyArg = sub_0806639C()` **before** `done = 0` forces r5 for
  `done` but scrambles prologue (6639C → `add r4,r0` → `mov r5,#0` → CpuSet vs retail
  `movs r5,#0` → CpuSet → 6639C). No C reorder recovered both.

## 2026-09-21 — allocator analysis

13 differing halfwords, all from one root cause: agbcc gives `done` **r4**, retail gives it
**r5**. Consequence: at `blend1` retail keeps the two zero stores in `r3`/`r4`
(`movs r4,#0` / `movs r3,#0`), agbcc has no callee-saved reg free so it rematerialises the
`strb` value as an extra `movs r0,#0` (+2B) and the `bge` offset shifts.

Everything else (sp-relative `state` access, 0x328 frame prologue, epilogue `movs r3,#0xCA;
lsls r3,r3,#2; add sp,r3`) matches exactly.

Tried, all still 319/336: `f8`/`f16` typed `u8`/`u16` locals in `blend1` (to defeat CSE of
the two zeros), swapped flag init order, `done` declared first, `r0 = r0 + r3` operand order,
combined variants. `old_agbcc` → 315/336. No `-f*` flag helps.

Retail's r4 is used *only* for the `blend1` zero (a 4-instruction live range) — the short-lived
constant must be allocated to a callee-saved register, which agbcc 2.9 does not do once `done`
holds r4. Needs an allocator behaviour we cannot express in C.
