# sub_08040EF4 — WIP

| | |
|--|--|
| ROM | `0x08040EF4` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08040EF4.c` |
| Last `match_function.py` | 13/88 (readable seed; matched still Thumb)
| Sibling / types | |

## Role

Keyed ROM-table lookup: find an `Unk40EF4` row by key, then index its value pointer with `gMainWorkPtr->unk1818`.

## Process

- 2026-09-20 — parked. Status: best 34/88 size-mismatch after raw offset and full register pins; baseline was 13/88 at 72B, while retail requires r5 key, r4 byte offset, r2/r3 table cursors, and r6 base+4 without the extra saved registers

## Current state

Semantic rewrite using `struct Unk40EF4` and a plain `do`/`while` walk — no register names or asm-style labels. Matching still needs agbcc to emit the retail r2/r3/r4/r6 cursor shape; permuter from this seed if same-size.

## Next

Re-score with `match_function.py`. If same-size DIFF, run permuter on this readable seed rather than reverting to goto/register mimicry.
