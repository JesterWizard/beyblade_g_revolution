# sub_0804A028 — WIP

| | |
|--|--|
| ROM | `0x0804A028` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_0804A028.c` |
| Last `match_function.py` | 15/174 then 55/174
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two semantic attempts: wrong-offset seed 15/174, corrected-offset render sequence 55/174 (172B). Correct fields are +0x2D5 and +0x2FC, global flag is MainWork +0x1855, and all four draw calls/constants are mapped; remaining differences are register allocation (retail keeps r4/r5 byte values and uses r1 for 0x50000) plus a two-byte branch/layout delta.

## Current state

Two semantic attempts: wrong-offset seed 15/174, corrected-offset render sequence 55/174 (172B). Correct fields are +0x2D5 and +0x2FC, global flag is MainWork +0x1855, and all four draw calls/constants are mapped; remaining differences are register allocation (retail keeps r4/r5 byte values and uses r1 for 0x50000) plus a two-byte branch/layout delta.

## Next

Restore corrected semantic seed and use targeted register/source-order/permuter search; preserve the exact fixed-point forms and seek retail r4/r5 values with the 0x50000 temporary in r1.
