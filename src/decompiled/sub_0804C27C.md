# sub_0804C27C — WIP

| | |
|--|--|
| ROM | `0x0804C27C` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_0804C27C.c` |
| Last `match_function.py` | compile error then 56/168
| Sibling / types | |

## Role

(one sentence)

## Process

- 2026-09-20 — parked. Status: Two attempts consumed: parameter-register syntax was rejected by agbcc; the viable local r4/r6 pin produced 56/168 (176B). Renderer semantics/constants are mapped, but fixed-register declaration order swaps the retail saved-register prologue and coordinate lifetime.

## Current state

Two attempts consumed: parameter-register syntax was rejected by agbcc; the viable local r4/r6 pin produced 56/168 (176B). Renderer semantics/constants are mapped, but fixed-register declaration order swaps the retail saved-register prologue and coordinate lifetime.

## Next

Use ordinary saved-register allocation/permuter work (input r4, image r7, first coordinate r6) rather than parameter asm syntax or the current fixed-register declaration order.
