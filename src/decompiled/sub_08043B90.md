# sub_08043B90 — WIP (67/76)

| | |
|--|--|
| ROM | `0x08043B90` |
| Retail | 76 bytes |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08043B90.c` |
| Last `match_function.py` | 67/76 (88.2%) |
| Sibling | `sub_08043BDC` (MATCH, walks `unk16E4` instead) |

## Role

Search the `MainWork.unk16E0` linked list of `{void *unk00; s32 unk04}` records for an entry whose `unk00` string matches `unk16C8` via `sub_08073440`. Returns the matched `unk04` index, `-1` if the list pointer is null, `0` if no entry matches.

## Process

- 2026-09-20 — parked after GCC asm-label match was stripped
- 2026-09-21 — struct seed improved 36/76 → 67/76; permuter randomization regresses (best 200)
- 2026-09-24 — re-attempted: 40+ C variants (key-in-while, goto head, arithmetic zero returns, volatile, old_agbcc, `-fprologue-bugfix`); all floor at 67/76 or regress. Confirmed via objdump: compiled tail has extra `movs r0,#0` at 0x3c; retail falls straight to `pop {r4}`.
- 2026-09-25 — goto+`return key` same_size 27/76. Keep 67/76 seed. Extra `movs r0,#0` still the only gap vs sibling `sub_08043BDC`.

## Current state

**Not matched.** Single-instruction epilogue gap: retail reuses `r0` from the loop-head `ldr r0,[r4]; cmp; bne` when the list is exhausted; agbcc always materialises `return 0` as `movs r0,#0`. Historical match used `while ((r0 = ...) != 0) return (s32)r0` with `r0` pinned — banned in semantic C now.

## Next

Hand-force loop-head register reuse without `register`/asm barriers, or accept readable Thumb until agbcc/permuter finds a 76B shape. **Do not** run randomizing permuter on this seed.
