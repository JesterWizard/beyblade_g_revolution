# sub_0803715C — WIP

| | |
|--|--|
| ROM | `0x0803715C` |
| Retail | 444 B. `s32 (u8 a, u8 b)`: scale a value with `_080740B0(..., 10)`, clamp to 20..100, then add it into `MainWork.unk0874` and an `Unk3715C` slot |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_0803715C.c` |
| Last `match_function.py` | 33/444, compiled 424 |
| Sibling / types | `struct Unk3715C` at `Unk002A0Record.unk24`; `struct Unk3715CTail.unk26`; `sub_08042C3C` |

## Role

Builds a 20..100 score from either `gMainWorkPtr->unk0874` (`b == 0`) or `records[b].unk08`, then folds it into the main-work counter and the per-slot counter. `a == 0` consults `sub_08042C3C((s8)_080672A8())` and may replace the score with `sub_080628B4(10) + 1`.

## Process

- 2026-09-21 — transcribed from the asm into `src/decompiled/sub_0803715C.c`. Types added: `Unk3715C`, `Unk3715CTail`, `Unk002A0Record.unk24`. `make compare` stayed OK (the linked file is still the Thumb wrapper).
- Score 33/444, 20 bytes short. First delta: `a` is kept in r4 (`lsrs r4, r0, #24`) instead of retail's `mov r8, r0`.

## Current state

Algorithm is in the seed. 33/444, size mismatch 424 vs 444.

## Next

Do not hand-chase the r8 allocation. Revisit only if a same-size seed shows up, then run the permuter.
