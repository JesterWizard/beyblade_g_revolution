# sub_0802B994 — WIP

| | |
|--|--|
| ROM | `0x0802B994` |
| Retail | |
| `src/matched` | **semantic C** |
| Seed | `src/decompiled/sub_0802B994.c` |
| Last `match_function.py` | 58/58 — MATCH (`-fprologue-bugfix`) |
| Sibling / types | |

## Role

Kill/update hook: look up the MainWork record at 0x0807709C by key on the 3-byte stride and refresh the caller's stored pointer.

## Process

- 2026-09-20 — parked. Status: matched only with GCC asm labels; stripped DIFF
- 2026-09-21 — **matched**. Extra `&row` local makes the loop re-read the record pointer each
  iteration, which is what turns the second table address into `adds r1, r2, #4` instead of a
  second pool literal (the plain form was 64 bytes with a nop tail).

## Current state

matched — semantic C in `src/matched/` (100%).

## Next

done.
