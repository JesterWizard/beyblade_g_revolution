# sub_08043B58 — WIP

| | |
|--|--|
| ROM | `0x08043B58` |
| Retail | 54B, `bx lr` leaf; NULL-terminated `Unk447CC *` table at `0x08096794` |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08043B58.c` |
| Last `match_function.py` | 47/54 bytes |
| Sibling / types | `Unk447CC`; caller `sub_080447CC` |

## Role

Look up the current `unk1690->unk00` key in the ROM pointer table at `0x08096794`.

## Process

- 2026-09-20 — “matched” with `register … asm("rN")` (banned). Honest C without labels: 47/54 same-size. Key landed in `r3` instead of `r2`; table cursor used `adds r2,#4` instead of `adds r3,r0,#4` / `ldm r3!,{r1}`.
- Goto/`r0` reuse retry: 41/54 size mismatch (extra `push {lr}`).
- 2026-09-21 — bounded permuter runs (300 s then 900 s, `--no-integrate`): no score 0
  (score 45 → 20). Remaining gap: r3/r4 hint order; input normalization before the flag test.

## Current state

not matched — parked. Best permuter score 20 (from 45); `src/matched/` holds readable Thumb.

## Next

Permuter on the 47/54 seed (no `asm` labels). If still DIFF, keep Thumb.
