# sub_08039BD4 — WIP

| | |
|--|--|
| ROM | `0x08039BD4` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08039BD4.c` |
| Last `match_function.py` | 293/1552, compiled 1512 |
| Sibling / types | |

## Role

Rebuilds the eight-row part menu: frees the screen's text slots, then for each 16-byte row draws the selected row in full and the others as a label plus cursor bar.

## Process

- 2026-09-25 — compiled size reached 1552 (same_size, 256/1552). Selected and unselected cursor calls are in each arm (0x0E / 0x0F), row is reloaded from the menu globals at the label epilogues, and the part id is hoisted before the negative-row skip.

## Current state

decomp.me `agbcc -O2` is 27186/67100 (59.48%), up from 28596/67100 (57.38%). Dropping the hoisted `&unk2A0` lets the last slot clear use `r4`. A single `rowLabel = rowIndex + 6` stops the `(index + 6) << 16` strength reduction. The setup still spills the index (`str r0, [sp, #0x10]; mov r9, r0`) where retail does `mov r9, r0; mov r8, r0`, and the function is 1512 bytes against retail 1552.

## Next

A 120s permuter run started at 47020 and stopped at 25430. It does not move `&unk2A0` out of `r4` or put the row index in `r9`.
