# sub_08039BD4 — WIP

| | |
|--|--|
| ROM | `0x08039BD4` |
| Retail | |
| `src/matched` | readable Thumb |
| Seed | `src/decompiled/sub_08039BD4.c` |
| Last `match_function.py` | 272/1552, compiled 1552, same_size |
| Sibling / types | |

## Role

Rebuilds the eight-row part menu: frees the screen's text slots, then for each 16-byte row draws the selected row in full and the others as a label plus cursor bar.

## Process

- 2026-09-25 — compiled size reached 1552 (same_size, 256/1552). Selected and unselected cursor calls are in each arm (0x0E / 0x0F), row is reloaded from the menu globals at the label epilogues, and the part id is hoisted before the negative-row skip.

## Current state

Compiled size matches retail (1552). A 300s permuter run started at score 32360 and finished at 21990, not 0. First mismatch is still the prologue: retail is `sub sp, #0x20` / `mov sl, r0`; the seed is `sub sp, #0x24` / `mov r9, r0`. The parameter is now `struct Unk39BD4 *`, which dropped one stack slot versus `void *`.

## Next

The index still lives on the stack, so agbcc strength-reduces `(u16)(rowIndex + 6)` into an extra slot and keeps the screen pointer in r9. Retail keeps the index in r9 and the screen pointer in sl. The permuter cannot rename those registers.
