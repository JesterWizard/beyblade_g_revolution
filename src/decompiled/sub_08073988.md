# sub_08073988 — MATCH

| Field | Value |
|-------|-------|
| ROM | `0x08073988` |
| Symbol | `TextMeasureWidth` |
| Retail | 96 B — NUL-terminated text width calculator (space / `\a` / `\b` / `\n` / glyph table) |
| Status | **96/96 MATCH** (`/* match-compiler: old_agbcc */`) |
| Seed | `src/decompiled/sub_08073988.c` (readable switch; integrated to `src/matched/`) |

## What unlocked the match (Jiang)

1. **`const u8 *` parameters** — `void *` + local copies changes prologue register order; retail keeps `r0`/`r1` as text/base.
2. **`old_agbcc`** — newer agbcc folds the `gData_080BB748` pool load into `r2`; old_agbcc emits retail's `ldr r0,=pool; adds r0,r1,r0`.
3. **Switch + `base_arg[gData_080BB748[ch]]`** — no goto chain or `index = (r0 = pool)` side-effect trick needed.

Prototype updated in `include/unknown-functions.h` to `const u8 *` for both pointer args.
